<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Required by Hardened Runtime for Electron's JIT-compiled V8 code.
       Both entitlements are needed for notarization to succeed; without
       them Apple rejects the build with "The executable does not have
       the Hardened Runtime enabled". No other entitlements are needed —
       this app bundles no native modules. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
</dict>
</plist>
