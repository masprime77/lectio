## Unreleased

- Completed the mobile Moodle import flow with the **per-week triage screen** (Phase 16 Part C, mobile), the step that turns fetched Moodle content into real readings and tasks. Each mapped week gets one row — section name, item count, an editable week-number field pre-filled from the same German date-range suggestion algorithm as desktop's Part C (reimplemented locally in TypeScript, not in `@lectio/core`, since it's UI-suggestion logic specific to this screen) — and a Skip/Read/Task selector defaulting to **Skip**, so nothing imports without an explicit per-week decision. Every surviving item in a non-skipped week becomes one reading (or one task, no due date) via `PlannerCore.addItem`, then the whole batch saves through the same conflict-aware `saveWithConflict` path every other mobile form uses. Validation is all-or-nothing: an empty or out-of-range week on any non-skipped row blocks the whole import and names the offending section, matching desktop's Part C behavior. On success, `router.dismissAll()` closes both the `moodle-import` and `moodle-triage` modal screens in one call, landing back on `/moodle`. As with earlier parts, `moodleModuleId`/`moodleSource` stay on the in-memory mapped item only and are never persisted onto the created reading/task — `addItem`'s stored shape is unchanged, and merge/dedup across accounts remains an explicit non-goal.
- Surfaced **Import from Moodle at its two file-import entry points** on mobile (Phase 16 Part B.1, mobile): a per-course "Import from Moodle" action in the semester detail screen's course action sheet, and a matching "Import a course from Moodle" row on the add-sheet's Course tab, next to the existing file-import rows. Both share a new `ensureMoodleAccountOrPrompt` guard that offers to navigate to `/moodle` when no account is connected yet, instead of opening an empty picker. `/moodle-import` (Part B) now accepts optional `semesterId`/`courseId` query params to preset itself: the per-course entry point locks both the semester and target-course pickers (visibly dimmed) since a course can't move between semesters through this screen, while the add-sheet entry point only preselects the semester, leaving the target-course choice — including "+ New course" — fully open, matching the existing file-import row's scope. Unlike desktop's Part B.1, mobile has no unsaved "draft semester" case to special-case: `add.tsx`'s Course tab only ever adds into an already-saved semester, so both entry points reuse `/moodle-import` directly with no parallel draft path.
- Added the **Import from Moodle** course-picker screen to mobile (Phase 16 Part B, mobile): pick a semester, a connected account (only shown when more than one is connected), a Moodle course, and a target Lectio course (existing or a newly-created one), then fetch and map that course's content and hand off to the Part C triage screen. Every fetched item is tagged with `moodleSource` (the chosen account's base URL) so two accounts can never collide on `moodleModuleId`. Creating a new target course persists it immediately via the existing conflict-aware `saveWithConflict` path, mirroring `CourseFields.tsx`'s save pattern; picking an existing course creates no duplicate. Added a small transient, in-memory `import-session.ts` module (never persisted) to hand the mapped content off to the not-yet-built triage screen — the mobile equivalent of desktop's `moodleImport` global. Also added TypeScript types for `@lectio/core/integrations/moodle` (the pure content mapper) alongside Part A's `integrations/moodle-client` types.
- Added a **Moodle accounts** screen to mobile Settings (Phase 16 Part A, mobile), the mobile equivalent of desktop's Phase 16 Part A: lists every connected account (built on the existing `moodle-token-store.ts`), lets you connect a new one via SSO with the token verified (a live `core_webservice_get_site_info` call) before it's stored, and disconnect any one independently. Reconnecting an already-connected `baseUrl` replaces that row in place rather than duplicating it. Added TypeScript types for `@lectio/core/integrations/moodle-client` (a new `paths` entry into the existing `types/lectio-core.d.ts` shim) so the screen can build a client and typecheck cleanly. No course fetching or import UI yet — that's Part B and Part C, mirroring desktop's sequencing.
- Fixed the Moodle triage screen silently doing nothing when "Import selected" was clicked without first setting a week's mode. Every week defaults to Skip, so clicking through without touching a mode dropdown or a batch button created **zero** items — the modal still closed and a native `confirm()` dialog still fired ("Imported 0 item(s)…"), but that dialog is easy to miss, and a draft course row in the New Semester modal shows nothing but its name and colour, with no reading/task count anywhere to reveal the difference between a real import and a no-op one. The fix blocks this the same way an invalid week number is already blocked: an inline error — "Choose Reading or Task for at least one week before importing." — keeps the triage modal open instead of quietly succeeding at nothing.
  Found by reproducing the exact create-mode flow (＋ New → Courses tab → Moodle → Fetch content → Import selected) with real, trusted mouse clicks rather than programmatic ones, against a live Electron instance. One early reproduction attempt produced a different-looking symptom — two duplicate draft courses from a single click — which turned out to be self-inflicted: manually invoking `init()` a second time for test setup bypassed the app's own `appStarted` guard (`showApp()`) and duplicated every listener `init()` registers, including the "Fetch content" button's. Confirmed unrelated to the real bug by checking that `bootstrap()`'s `handleSession()` already guards against this exact double-init risk in production (its own comment names the scenario: an `onAuthChange` `INITIAL_SESSION` event duplicating the startup `getSession()` call) — real usage only ever calls `init()` once. The actual bug was isolated by instrumenting the button's own click count in the capture phase (confirming exactly one real click event) while it still produced zero items, then comparing two screenshots — one course that received real items and one that received none — and finding them visually identical.

- Added an **Expand all / Collapse all** control to the Moodle triage screen, so a course's items can be reviewed without clicking every week's chevron. It's one button rather than two: it reads the rows' actual state, so it offers "Collapse all" only once every week really is open — including when the last one was opened by its own chevron — and flips back the moment one is closed again. From a half-open state it expands rather than collapsing, which is the more useful default when you've opened a couple of rows and want the rest.
- Gave the Moodle triage screen **real batch control** (Phase 16 Part D), after Part C proved tedious on a course with a dozen weeks. Four changes: an "All weeks: Skip / Reading / Task" row that sets every week's mode in one click (any row can still be changed by hand afterwards — it's a starting point, not a lock); **sequential week auto-fill**, so typing a week number into any row fills every row below it with incrementing values, leaving rows above untouched and re-cascading if an earlier row is edited again — weeks in a Moodle course are nearly always consecutive, so this replaces typing a number into every single row; **per-item selection**, where each week expands to show its items with individual checkboxes (all ticked by default, so an untouched row behaves exactly as it did before) plus "Select all" / "Clear all" spanning every week at once; and a **loop back** offer after a successful import, so importing several courses in a row no longer means reopening the picker from Settings each time.
  Two behaviours worth spelling out. A week now contributes only if it isn't skipped *and* still has items ticked, so a week whose items were all deselected creates nothing — and, deliberately, filtering happens *before* week-number validation, so such a week can't block the import over a week number that no longer matters. Everything Part C established otherwise stands: the suggestion algorithm is untouched, the "From `<account>`" line stays, and validation is still all-or-nothing across every contributing row. The loop-back re-enters the way the import was entered — a create-mode import hides the New Semester modal again and reopens the picker against the draft, so its target list and write path stay pointed at the draft rather than at whatever semester is loaded behind it.
  Verified with a 10-case standalone check of the cascade (editing the first row, rows above staying put, re-cascading from an earlier row, clamping at the semester length, and blank/zero/negative bases correctly doing nothing) plus a live run: batch buttons set all four rows and a per-row override survived; the cascade produced 5-6-7-8, then 11-12 below an edited third row while 5-6 above it held, then re-cascaded to 1-2-3-4; expanding a week showed its items, unticking one moved the collapsed summary to "2 of 3 items", and Clear all / Select all swept every week. An import with one item deselected created exactly the ticked items, while two non-Skip weeks with nothing selected created nothing and — with blank week numbers — did not block the rest. Both loop-back answers were exercised against the **real** Electron `confirm()` dialog rather than a stub (worth confirming, given `prompt()` is unimplemented in Electron and had to be worked around in Part A): declining closed out, accepting reopened the picker, and the create-mode variant reopened against the draft with `persist` called zero times.
  Fixed one bug this surfaced that no DOM assertion would have caught — only the screenshot did: the modal's pre-existing `.modal label input { width: 100% }` rule, meant for its stacked text fields, was inherited by the new per-item checkboxes, stretching each one across the row and squeezing every item name to zero width. The checkboxes now set `width:auto` explicitly and the name takes the remaining space with ellipsis.
- Completed the Moodle import with the **per-week triage screen** (Phase 16 Part C), the step that finally turns fetched Moodle content into real readings and tasks. Each mapped week gets one row — section name, item count, an editable week-number field, and a Skip / Reading / Task choice that defaults to **Skip**, so nothing is imported without an explicit decision. The choice is per *week*, not per item: every surviving item in a week becomes one reading (or one task, with no due date — `core_course_get_contents` doesn't supply one) in the target course, titled from the module name. Items are created through `PlannerCore.addItem` rather than hand-rolled, so Moodle-sourced readings and tasks get exactly the same ids and default statuses (`r-pending`/`t-pending`) as hand-added ones, through code the core tests already cover. A header line names the account the batch came from, which matters once more than one is connected. Validation is all-or-nothing: an empty or out-of-range week on any non-skipped row blocks the whole import and names the offending section, so a bad number can never leave a half-finished import behind.
  Week numbers are suggested from the German date range the Phase 14 mapper parsed, with the user free to override. Because those ranges carry no year, the suggestion tries the semester's start year first and rolls to the next year when that would land the section before the semester began — which is what makes a winter semester's "13. Januar" section resolve correctly instead of landing ten months early. The result is clamped into `[1, weeks]`, and a section with no parseable range simply comes up blank for manual entry. This lives in `app.js`, not `@lectio/core`: it's a UI suggestion for this screen, not a reusable pure transform.
  Two things the spec didn't cover, both needed because of the create-mode entry point added earlier in this phase. The triage screen now measures weeks against a **semester context** rather than `state.semester` directly — during a create-mode import there is no saved semester, and its start date and length are still unsaved values in the New Semester form, so they're read from there; `state.semester` at that moment is whatever happens to be open behind the modal, which would have produced wrong suggestions against the wrong week count. And a create-mode import must **not** call `persist()`/`render()`, which would save whichever semester is loaded in the background; the items ride along on the draft course until the user clicks Create. `suggestWeekFromDateRange` also guards a missing or unparseable start date (returning no suggestion rather than `NaN`), which is exactly the state a half-filled create form is in.
  Verified against a live renderer plus a standalone 15-case check of the date algorithm covering week boundaries, the winter-semester year rollover, clamping, and every degenerate input. In the app: rows pre-filled 1 and 2 from real date-range fixtures and blank for a topic-named section; a Reading week produced two readings in week 1 and a Task week one task in week 2, all visible in the planner under the right week and section, with the skipped section contributing nothing; blank and out-of-range weeks each blocked the import with the section named and **zero** items created. Importing the same Moodle course from a *second* account into the *same* Lectio course created both batches independently without crashing (no dedup is attempted — merge between accounts remains an open product decision), and re-running the flow produced fresh rows reset to Skip with exactly one alert, confirming the `.onclick =` style replaces handlers instead of stacking them. The create-mode path was confirmed to take its suggestions from the form (weeks 2 and 3, max 8) rather than the loaded semester (which would have given 1 and 2, max 15), to call `persist` zero times, and to survive `submitModal` into the saved payload. As before, the run used a scratch semester with `persist` stubbed and the save intercepted, so nothing was written to the real account. As designed, `moodleModuleId`/`moodleSource` are **not** persisted onto created items — confirmed on the created objects — since `addItem` has no field for them and changing core's stored shape is out of scope here.
- Made the Courses tab's **Moodle button work while creating a semester**, closing the gap Part B.1 deliberately left open — the ＋ New → Courses tab now offers Moodle import alongside the file "Import", not just when editing an already-saved semester. Reading the draft model turned out to make this far cheaper than Part B.1 assumed: `closeModal()` only *hides* the overlay (it never resets the form or the draft), and `importCourseFromModal` already had the exact create-mode pattern to follow — push the course object into `state.editingSemester.courses` and add a matching row via `addCourseField`, keyed by the row's `dataset.courseId`, which is precisely what `submitModal` walks on Create to preserve an imported course's readings and tasks. So instead of a parallel data path, the picker gained a `draftMode` flag: it skips the "open a semester first" guard (create mode is exactly the case where there deliberately isn't one), lists the modal's own **course rows** as import targets rather than `state.semester.courses`, and writes into the draft without calling `persist()` or re-rendering — nothing is saved until Create, as it should be. A target row the user typed but never imported into is promoted to a real draft course on the fly (keeping its typed name and colour) so the triage step always has a course object to attach items to; a row id minted for this that ends up unused is simply ignored by `submitModal`, which mints a fresh one for any row it can't match. Because the picker is launched from a modal holding unsaved work, `closeMoodleCourseModal` now restores the New Semester modal on **every** close path — Cancel, backdrop, and completed fetch — so an in-progress semester can't be stranded behind the picker. Edit mode is untouched: it still persists immediately and still doesn't reopen the semester modal afterwards. `moodleImport.draft` records which mode an import is in, since the triage step must read it before writing items anywhere.
  Verified in a live renderer over the DevTools Protocol, starting from the real `openCreateModal()`: the button now shows on Courses in **both** modes and stays hidden on the other three tabs in each; a create-mode import kept the half-filled form intact (name, start date, weeks) and the typed course row, added the imported course as a second row, called `persist` **zero** times, and left the loaded semester untouched. Importing into the typed row promoted it in place without duplicating it. Cancel and backdrop both restore the semester modal; edit mode still doesn't. The decisive check ran `submitModal()` with the save intercepted: it produced a semester carrying both draft courses, with a reading attached to the Moodle-imported one surviving complete with its `moodleModuleId` and `moodleSource` — proving the draft path reaches disk correctly without anything actually being written to the real account. (One early run showed a doubled import; that was an artifact of the harness calling `init()` a second time and double-binding the listener, not a defect — a clean run imports exactly once.)
- Surfaced **"Import from Moodle" where courses are actually imported** (Phase 16 Part B.1), instead of leaving it buried in Settings: a graduation-cap icon in each course column's header row on the dashboard, and a "Moodle" button in the New/Edit Semester modal's Courses tab footer next to the existing file "Import". The two behave differently on purpose — the per-course icon already knows its target, so it opens the picker with the target course **preset and disabled** and the new-course-name field hidden (`openMoodleCourseModal` gained an optional `{ presetCourseId }`), while the footer button opens the full picker with the whole "Import into" dropdown, since from there no particular course is implied. Both share one guard: with no account connected yet they offer to open Settings (where Part A's "Moodle accounts" section lives) rather than opening an empty picker, and declining leaves everything as it was — notably the semester modal stays open, since the guard returns before `closeModal()`.
  The footer button is deliberately shown **only on the Courses tab and only when editing an existing semester**. Create mode is excluded by design, not oversight: a not-yet-saved semester accumulates courses in a `state.editingSemester` draft rather than `state.semester`, and its `startDate`/`weeks` are still unsaved values sitting in form inputs, so importing there would need a second, parallel data path through Parts B and C — disproportionate when saving the semester first is one click away and then the Edit-mode entry point applies. Worth revisiting as its own part if it proves annoying in practice.
  Verified in a live Electron renderer over the DevTools Protocol: the footer button's visibility was checked across **all eight tab × mode combinations** (visible only in edit/courses, hidden in all four create-mode tabs); the per-course icon appears on every course between file-Import and Delete; clicking it presets and disables the target (and a disabled `<select>` still reports its value, so the import correctly lands on that course and creates no duplicate); the footer button closes the semester modal first and opens the picker with the target enabled and defaulted to "+ New course" — which also proves the preset state resets between openings. The guard was exercised in both directions by temporarily removing every stored account: declining opens nothing, accepting opens Settings with the Moodle section present, and neither opens the picker. That test round-tripped the user's real connected account through save-and-restore inside a `try/finally`, with the token never leaving the renderer, and it was confirmed restored afterwards. As in Part B, the run used a scratch semester and a stubbed `persist`, so no real semester data was written.
- Wired desktop's "Import courses…" button to a real **Moodle course picker** (Phase 16 Part B): choose which connected account to browse, which of its enrolled Moodle courses to import, and which Lectio course to import into (an existing one, or a new one created on the spot), then fetch that course's contents and run them through `@lectio/core`'s pure mapper. It deliberately stops with the mapped `{ weeks: [...] }` held in memory — Part C builds the per-week triage UI that turns those into readings and tasks, and until it exists the flow ends in a placeholder alert reporting the week count. The account row is **hidden when only one account is connected** (the common case shouldn't cost an extra click) and switching accounts re-fetches that account's course list against that account's own token. Critically, `mapCourseContents` is called with `{ source: <account baseUrl> }`, so every imported item carries `moodleSource` — this is the half of Phase 14's collision-avoidance design that only pays off here, since `moodleModuleId` is unique only *within* one instance and two connected accounts can hand back the same id for entirely different files.
  Two deviations from the written plan, both from reading the surrounding code rather than assuming: `addCourse` turned out **not** to be in `app.js`'s existing `window.PlannerCore` destructure (it has `addCourseButton`/`addCourseField`/`addCourseColumn`, none of which are it), so it was added there rather than left to fail at runtime; and new courses take their colour from the app's own `DEFAULT_COLORS` palette by position — the same rule the semester editor's course rows already use — instead of a hardcoded off-palette blue. Also added a guard for the **no-semester-loaded** case: the import writes into `state.semester.courses`, which starts as `null`, and without it "Fetch content" would have thrown `Cannot read properties of null (reading 'courses')` *after* spending a full Moodle round trip. That throw was confirmed in the live renderer, not assumed.
  Verified end-to-end against a real running Electron instance over the DevTools Protocol, with the real `window.moodleAuth` IPC bridge and the real core mapper in play and only the Moodle *network* client faked (no instance or credentials available here). Confirmed: with 3 accounts the picker lists all of them and defaults to the first; switching accounts rebuilds the course list using that account's own token (checked per-account, `tok-a` vs `tok-b`); the target dropdown always offers "+ New course" plus every course in the loaded semester; the name field appears only for "+ New course"; a successful fetch creates exactly one course, maps the fixture to 2 weeks (correctly dropping both an item-less section and a hidden one), and tags **every** item with the selected account's `moodleSource`; importing into an existing course creates no duplicate; and the same `moodleModuleId` (612) fetched from two different accounts stays distinguishable purely by `moodleSource`. Error paths all surface as inline red text with the modal still open and no course created — an empty course name, and a Moodle `invalidtoken` failure (which, because course creation happens only after the fetch resolves, leaves no orphan course behind). The user's real signed-in Supabase semester was never written to: the run swapped in a throwaway semester object and stubbed `persist`, and the two scratch Moodle accounts added through the real IPC store were removed afterwards, leaving their genuine connected account untouched.
- Added a **Moodle accounts** section to desktop's Settings modal (Phase 16 Part A), the first renderer UI for the multi-account storage Part 0 built — connect any number of Moodle accounts, see each as its own row, and disconnect any one independently. Vendored `@lectio/core`'s `integrations/moodle` and `integrations/moodle-client` next to `index.html` via `scripts/sync-core.js` (as `window.LectioMoodle`/`window.LectioMoodleClient`, git-ignored like the other vendored core files) so the renderer can build a Moodle client without a `require()` it can't make under `contextIsolation:true`. Connecting an account runs `window.moodleAuth.captureToken` (the real SSO `BrowserWindow` flow from Phase 15/Part 0, untouched by this change) and then **verifies the token** with a live `core_webservice_get_site_info` call before storing anything — an unverified token is never persisted — using the response's `fullname`/`username` as the account's display label. Reconnecting a `baseUrl` that's already connected replaces that row in place rather than duplicating it, matching Part 0's storage semantics exactly. The "Import courses…" button (inert placeholder for Part B) only appears once at least one account is connected.
  Deviated from the original spec in one place, discovered only by actually running the feature rather than reading the code: **Electron does not implement `window.prompt()`** — `alert()`/`confirm()` work, but `prompt()` always throws `"prompt() is not supported."`, a long-standing Electron limitation (there's even a pre-existing, likely dormant `prompt('Tag name:')` elsewhere in `app.js` hitting the same wall, unrelated to this change and left alone). Base-URL entry is an inline text input + Connect/Cancel revealed under "+ Add account" instead of a blocking `prompt()`, styled to match the rest of the Settings modal, with Enter-to-submit and Escape-to-cancel.
  Verified end-to-end against a real running Electron instance via its `--remote-debugging-port` (driven directly over the DevTools Protocol, since the sandboxed preview browser can't reach an external CDP socket): a real click opens the real SSO `BrowserWindow` past the app's own sign-in gate, and closing it before completing login shows the exact inline error with no crash and no row added — proven twice, once with the original `prompt()` implementation and once after the inline-form fix, so the regression risk of touching that flow's caller is closed. The successful-connect path was verified with a temporary, env-gated (`LECTIO_TEST_FAKE_SSO`), fully-reverted stub of just the network- and SSO-dependent halves (`captureMoodleToken`'s real window, and the outbound `core_webservice_get_site_info` fetch) — everything else in the run, including the actual `window.moodleAuth` IPC round trip to `main.js`'s real `safeStorage`-backed store, was exercised for real: adding two distinct accounts produced two independent rows, reconnecting the first left the account count at 2 (no duplicate), and disconnecting each in turn removed only that row, hid "Import courses…" only once the last account was gone, and left the other account's row untouched throughout. `git diff` on `main.js`/`preload.js` confirms Part 0's IPC surface needed no changes for any of this, and a repo-wide grep confirms none of the temporary test scaffolding survived.
- Gave **mobile** the same multi-account Moodle token storage desktop just got, keeping the deliberate platform parity Phase 15 part C established — `src/auth/moodle-token-store.ts` now exposes `listMoodleAccounts`/`getMoodleAccountToken`/`addMoodleAccount`/`removeMoodleAccount` with the same names, the same `{ baseUrl, wstoken, label }` record, the same keyed-by-`baseUrl` semantics, and the same replace-in-place-on-reconnect behaviour as `main.js`, so the two platforms still read alike. All accounts live under **one** `expo-secure-store` key holding the same `{ accounts: [...] }` record desktop writes to its encrypted file, rather than a key per account: SecureStore keys are restricted to `[A-Za-z0-9._-]`, so per-`baseUrl` keys would need an encoding scheme and there'd be no way to enumerate them. The tradeoff, noted in the source, is Android's ~2048-byte guidance for a single SecureStore value — ample for the handful of instances one person is enrolled at (~130 bytes per account) but not a design that would scale to dozens. The key name is deliberately **unchanged**, so an install already holding a Phase-15 `{ baseUrl, wstoken }` token migrates transparently instead of silently losing it: the old shape is recognised on read and surfaced as a one-item list, and rewritten to the new shape only on the next mutation. Listing strips `wstoken` as desktop does, though here that's hygiene (a token can't leak into a log or a rendered list by accident) rather than the process-boundary isolation it buys on desktop. Unlike desktop, this half **is** covered by the test suite — `test/moodle-token-store.test.ts` grew from 4 tests to 14, pinning multi-account listing, token-free summaries, hit/miss lookups, in-place replacement, removal (including a no-op removal of an unconnected account), an unrecognised payload degrading to `[]`, and all four migration properties; it also pins the SecureStore key literal, so a rename that would strand existing installs' tokens fails a test. The SSO capture flow in `moodle-sso.ts` is untouched, and there are still **no UI consumers** on mobile and no token refresh/expiry/revocation handling.
- Lifted desktop's Moodle token storage from **one connection to several accounts**, the prerequisite for Phase 16's import UI — the real driver being two separate TU Darmstadt instances (`moodle.informatik.tu-darmstadt.de` and `moodle.tu-darmstadt.de`) that need to be connected at the same time. `<userData>/moodle-token.enc` now holds `{ accounts: [{ baseUrl, wstoken, label }] }` instead of a single `{ baseUrl, wstoken }`, keyed by `baseUrl` — a Moodle instance is the natural unique key here, since this is one person's own accounts on several instances rather than a multi-user handoff. `label` is display-only (the Moodle `fullname`/`username` the renderer captures at connect time) and never used for lookup. The four IPC channels are replaced accordingly (`moodle-list-accounts` / `moodle-get-account-token` / `moodle-add-account` / `moodle-remove-account`), and `window.moodleAuth` becomes `listAccounts`/`getAccountToken`/`addAccount`/`removeAccount`/`captureToken`. Listing deliberately **strips `wstoken`**, so populating a Settings list or an account picker no longer re-exposes every token on every call — the token is handed over only by an explicit per-account `getAccountToken`, which the renderer still needs to build its own `createMoodleClient`. Adding a `baseUrl` that already exists **replaces that entry in place** rather than accumulating duplicates, which is also what a reconnect after an expired token does. Phase 15's single-account files migrate transparently: the old shape is recognised on read and surfaced as a one-item list, and it's rewritten to the new shape only on the next mutation — a read-only call never rewrites the file. The SSO capture flow (`captureMoodleToken`, `buildLaunchUrl`, `parseMoodleMobileRedirect`) is untouched, as it's independent of how many accounts are stored, and the mapper's `moodleSource` tagging from Phase 14 is what will keep two accounts' items from colliding on `moodleModuleId` once the import UI passes a source through. Still deliberately **no renderer UI** (Part A wires it up) and no token refresh/expiry/revocation handling; mobile got the matching change separately (its own bullet). `packages/desktop` has no test runner, so this was verified with a throwaway harness that runs the five functions' real source from `main.js` against a temp file with `safeStorage` stubbed as a reversible pass-through: fresh install, two-account add/list, token-free listings, hit/miss lookups, removal, in-place replacement, migration (including that reads don't rewrite and the migrated token survives the first mutation), an unrecognised payload degrading to `[]`, and a machine without OS-backed encryption erroring rather than silently falling back to plaintext.
- Bumped desktop's pinned Electron from `31.7.7` to `43.2.0` after Apple revoked `31.7.7`'s notarization ticket, which was tripping a "Malware Blocked" Gatekeeper/XProtect alert on local macOS dev launches. The version bump itself (`packages/desktop/package.json` + a regenerated `package-lock.json`) landed separately; this change reconciles the root `package.json`'s `allowScripts` allowlist, which still pinned the old `electron@31.7.7` key and would otherwise have warned that `electron@43.2.0`'s install script is uncovered on a clean install. `main.js`/`preload.js` needed no changes — `contextIsolation: true`, `nodeIntegration: false`, and contextBridge-only exposure (no `remote` module) already held for both the main window and the Moodle SSO capture window. Verified with a clean `npm install` (the `got`/`extract-zip`/`yauzl`-based download stack `electron@31.7.7` used for its own install script is superseded by `undici` + `@electron-internal/extract-zip` in `43.2.0`, accounting for most of the lockfile's diff) and a full `npm test` pass.
- Gave the **mobile** app the same Moodle connect capability desktop got in part B (Phase 15, part C). `captureMoodleToken(baseUrl)` drives Moodle's SSO login through the system browser — `ASWebAuthenticationSession` on iOS, Custom Tabs on Android — and captures the `moodlemobile://token=…` redirect, reusing the exact `WebBrowser.openAuthSessionAsync` mechanism the app already uses for Google/Apple sign-in rather than introducing a second auth mechanism. Like that flow, it needs a dev build; the custom-scheme round-trip is unreliable in Expo Go. The captured token is stored via `expo-secure-store` (Keychain on iOS, Keystore-backed encrypted preferences on Android) behind `getMoodleToken`/`setMoodleToken`/`clearMoodleToken` — deliberately the same names and the same `{ baseUrl, wstoken }` record shape as desktop, so the two platforms read alike. It pointedly does **not** reuse the existing AsyncStorage adapter, which is plaintext on disk and unfit for a live account credential. Same deliberate limits as desktop: **no UI yet**, exactly **one connection at a time** (one fixed key, so re-connecting overwrites rather than accumulating), and **no token refresh/expiry/revocation handling**.
- Hardened the shared `@lectio/core` SSO helper for React Native before mobile started depending on it. `moodle-sso.js`'s base64 decoding required either `Buffer` (Node) or `atob` (browser) and threw when it found neither — and Hermes, React Native's engine, guarantees neither across versions, so the shared module could have failed at exactly the moment mobile first used it: on a real captured redirect, with no test having caught it. It now falls back to a small dependency-free manual decoder, scoped to this module's actual payload shape (ASCII tokens joined by `:::`) rather than being a general-purpose binary/unicode base64 decoder. Verified to produce byte-identical results to the `Buffer` path, and pinned by a test that stubs both globals away. `buildLaunchUrl` and `parseMoodleMobileRedirect` are unchanged.
- Taught the desktop app to **obtain and securely store a Moodle token itself** (Phase 15, part B), replacing the manual DevTools-capture process used during spiking. "Connect Moodle" now has a real capability behind it: `captureMoodleToken(baseUrl)` opens a dedicated `BrowserWindow` on Moodle's SSO login and intercepts the `moodlemobile://token=…` redirect on `will-redirect`/`will-navigate` — the redirect that would otherwise just fail, since nothing registers that custom scheme — then closes the window as soon as the flow settles, whether it succeeded, failed to parse, or the user cancelled by closing it. The captured `{ baseUrl, wstoken }` is written to `<userData>/moodle-token.enc` through Electron's `safeStorage`, so it's encrypted at rest by the OS keychain (Keychain on macOS, DPAPI on Windows) and **never touches disk in plaintext**; a machine without OS-backed encryption gets a clear error instead of a silent plaintext fallback. The whole surface is exposed to the renderer as a new `moodleAuth` preload namespace (`getToken`/`setToken`/`clearToken`/`captureToken`), a sibling of the existing `planner`/`updater`/`saver`/`settings` bridges, with no raw `ipcRenderer` crossing the boundary. Known limits, all deliberate: **no UI yet** (nothing triggers this from the interface — a later prompt wires it up), exactly **one connection at a time** (no multi-account storage, matching part A's single-account client), and **no token refresh/expiry/revocation handling**, which isn't designed yet.
- Added `@lectio/core/integrations/moodle-sso`, the pure, testable half of that flow: `buildLaunchUrl(baseUrl, options)` builds the `admin/tool/mobile/launch.php?service=…&passport=…&urlscheme=…` URL that starts Moodle's "Mobile app" SSO handshake (all three params overridable, random passport by default), and `parseMoodleMobileRedirect(url)` decodes the `<scheme>://token=<base64>` redirect it ends at into `{ wstoken, privatetoken, passport? }`. The redirect payload's shape **varies by Moodle version** and both shapes were seen against two real, separate instances — `moodle.informatik.tu-darmstadt.de` returned 3 `:::`-joined segments (`passport:::wstoken:::privatetoken`) while `moodle.tu-darmstadt.de` (Moodle 4.5.12) returned only 2, omitting the passport — so the parser handles both rather than assuming either. It also tolerates a URI-encoded payload as a real navigation event delivers it, accepts any custom `urlscheme`, and returns `null` rather than throwing for anything malformed, so a garbled redirect can never crash the capture window. Keeping this logic in core means the Electron layer stays thin (window creation, IPC, `safeStorage`) and the parsing rules are fully unit-tested — `packages/desktop` still has no test runner, so that split is what gives this flow real coverage.
- Started **Phase 15 (part A)** of the Moodle integration with `@lectio/core/integrations/moodle-client`, the shared network client that actually talks to Moodle's Web Services REST API. `createMoodleClient({ baseUrl, token, fetchImpl })` returns a client bound to one instance + token exposing `getSiteInfo()`, `getEnrolledCourses(userId)`, and `getCourseContents(courseId)` — exactly the three functions validated live in the Phase 13 spike; `mod_assign_get_assignments` is deliberately left out, since that spike found it unusable at TU Darmstadt and nothing in the design depends on it. It lives in core rather than a platform package because it needs only the `fetch` global (present in Node 22, browsers, and React Native/Expo alike), and `fetchImpl` is injectable in the same style as `createFsStorage`/`createSupabaseStorage`, so the whole surface is unit-tested without a real Moodle instance or a single network call. Missing `baseUrl`/`token`, or an environment with no `fetch` at all, fail **synchronously** at construction rather than at the first request, a trailing slash on `baseUrl` can't produce a malformed endpoint, and Moodle's `{ exception, errorcode, message }` envelope always raises a structured `MoodleApiError` carrying `errorcode` and `wsfunction` as fields — so a caller can treat e.g. `invalidtoken` as "please reconnect Moodle" without string-matching a message, and an error envelope is never mistaken for a successful response. Secure token storage and the SSO-derived token-capture flow are the platform halves of Phase 15 and land separately (parts B and C); this module only knows how to make a call once it already has a base URL and a token. See `docs/MOODLE_INTEGRATION_SPIKE.md`.
- Made the Moodle mapper's items safe to combine across **more than one Moodle account**. `moodleModuleId` is only unique *within* a single instance — confirmed by testing against two separate real TU Darmstadt Moodles that independently assign overlapping module ids — so `mapModuleToItem(mod, source)` and `mapCourseContents(sections, { source })` now take an optional `source` and attach it to each item as `moodleSource`, making `(moodleSource, moodleModuleId)` a collision-free key. It's opt-in and fully backwards compatible: omit `source` and items keep their exact three-key `{ name, url, moodleModuleId }` shape. This only *tags* items so a future re-sync can disambiguate — it deliberately implements no merge or conflict-resolution logic, which depends on Phase 15/16 decisions not yet made.
- Taught the Moodle PoC script to hold **several named accounts in one `.env`**, so testing a second instance no longer means hand-copying env files. Set `MOODLE_ACCOUNTS=a,b` plus a `MOODLE_<ID>_BASE_URL`/`MOODLE_<ID>_TOKEN` pair per account and select one with `--account=<id>` (optional when only one account is listed); output filenames then carry the account id so two accounts' dumps never overwrite each other. Leaving `MOODLE_ACCOUNTS` unset keeps the original behaviour and the original filenames byte-for-byte, so every already-saved dump stays valid. Unknown or missing accounts, and `--account` without `MOODLE_ACCOUNTS`, all fail with a specific error rather than being silently ignored.
- Added `spikes/moodle-poc/inspect-course.js`, promoting the ad-hoc scratch script used during manual Phase 14 testing into a committed dev tool. It reads a course dump `poc.js` already saved (single- or multi-account), runs it through the real `@lectio/core` mapper, and prints a per-section report — name, hidden/visible, importable-module count, and what the date-range parser made of each name — plus a summary line. It never touches the network, supports the same `--account=<id>` (reused as the mapper's `source`, demonstrating `moodleSource` end-to-end) and an independent `--source=<value>`, and lists whichever dumps *do* exist when the requested one is missing. Its `inspect-result.json` output is git-ignored, since it holds real course data.

- Fixed the Moodle date-range parser silently missing a **third real course shape**: `parseGermanDateRangeSectionName` was anchored to names that are *only* a date range, but course 43541 names all 14 of its weeks `"Woche 1: 22. April – 25. April"` — a well-formed range behind a `"Woche N: "` prefix — so every one fell back to raw section order and imported with no date at all (0 of 16 weeks parsed; now 14 of 16, the other two genuinely having no date). The pattern now searches for a range anywhere in the section name, and carries the German month names inside the pattern rather than validating them afterwards, so a structurally similar phrase (`"1. Woche - 2. Woche"`) can neither match on its own nor shadow a real range later in the same name. A whole-word month is required (`"13. Aprilx"` doesn't match). Added a third fixture to `moodle.test.js` built from that course's real response — it also covers `scheduler`/`lti`/`choicegroup` modules (none in the allowlist) and a *visible* section whose modules are all forums, which is correctly dropped as item-less.
- Completed **Phase 14 (part 2/2)** of the Moodle integration by extending `@lectio/core/integrations/moodle` with the week-grouping half of the mapper. `parseGermanDateRangeSectionName(name)` turns a section name like `"13. April - 19. April"` into `{ startDay, startMonth, endDay, endMonth }` (1-indexed months, no year — Moodle section names never carry one), tolerating an en dash, extra whitespace, and any casing of the German month name, and returning `null` — never throwing — for topic-named sections (`"Thema 00 - Einleitung"`, `"Allgemeines"`) or non-string input. `mapCourseContents(sections, options)` is the orchestrator: it drops hidden sections outright, filters each surviving section's modules through the Part-1 allowlist/visibility primitives, and returns `{ weeks: [{ moodleSection, sectionName, dateRange, items }] }` — where `moodleSection` is Moodle's own raw section number (the **documented fallback ordering** when the name doesn't parse as a date range, not a re-index) and `dateRange` is `null` rather than a fabricated date. Sections left with no importable items are dropped by default (they'd be noise in the Phase 16 triage screen) and kept only with `{ includeEmptyWeeks: true }`. Items still carry no week or type field of their own. Validated by new Vitest cases driven by two anonymized fixtures shaped like the real Phase 13 PoC courses — one topic-structured (no parseable dates anywhere) and one date-range-structured (including the real hidden trailing `"DMML Klausur"` / `"Einsicht"` sections).
- Started **Phase 14 (part 1/2)** of the Moodle integration with a new pure core module `@lectio/core/integrations/moodle` (`packages/core/src/integrations/moodle.js`, dual-mode like `lectio-file.js`: `window.LectioMoodle` in the browser, `module.exports` in Node; no DOM/Electron/fs/network). It ships the filtering and shaping primitives the mapper is built on: `IMPORTABLE_MODNAMES` (the `resource`/`url`/`folder` allowlist), `isModuleImportable` (allowlist + `visible: 0` / `uservisible: false` filtering), `isSectionVisible` (the same visibility rules for sections, so live courses' hidden trailing exam/review sections never surface), and `mapModuleToItem` (shapes a module into `{ name, url, moodleModuleId }`, always from `module.url` and **never** `contents[].fileurl`, so no Moodle token is ever persisted in a saved link). The module stays **type-agnostic** — it never classifies an item as a reading or a task; that's the user's choice in the Phase 16 import UI. Covered by `packages/core/tests/unit/moodle.test.js` using real (trimmed) module shapes from the Phase 13 PoC dumps, and exposed via a new `./integrations/moodle` package export.
- Started **Track C (Moodle integration)** with the Phase-13 design spike: a decision doc (`docs/MOODLE_INTEGRATION_SPIKE.md`) that weighs the three access models — Web Services REST API + user token, iCal export, and scraping — and recommends the token-based Web Services API as the primary integration, with iCal kept as a due-dates-only fallback and scraping rejected. It documents how Moodle maps onto Lectio's existing `Semester` shape (course → course, resources/assignments → readings/tasks bucketed into weeks, assignment due dates → task due dates), the four Web Services functions the integration relies on, an unchecked validation checklist to fill in after a live run, and the open risks (institution-dependence, varying course structure, token security). Also added a `## Moodle integration (Track C)` section to `docs/PENDING_FEATURES.md` tracking Phases 13–16.
- Closed out **Phase 13** with real validation findings from two PoC runs against `moodle.informatik.tu-darmstadt.de` (course id 10, an old topic-structured course; course id 1998, a live date-range-structured one). `docs/MOODLE_INTEGRATION_SPIKE.md`'s validation checklist is now ticked and carries the actual results: all Web Services calls work, but the instance is **SSO-backed** so `login/token.php` doesn't work and the token must come from the `admin/tool/mobile/launch.php` + `moodlemobile://token=` redirect capture (what Phase 15 must replicate in a WebView); section names parse as German date ranges on one course but are pure topic names on the other; hidden (`visible: 0` / `uservisible: false`) trailing sections exist on live courses; content arrives as `label`/`forum`/`choice`/`resource`/`url` modules; and each module exposes both a session-friendly `module.url` and a token-requiring `contents[].fileurl`.
- Reworked the spike doc's mapping and risk sections around the decision that the core mapper is **type-agnostic** — it no longer classifies items as readings vs. tasks (that choice moves to the user in the Phase 16 import UI). The mapper's scope is now: `modname` allowlist (`resource`/`url`/`folder`), `visible`/`uservisible` filtering, week-grouping by date-range parsing **with a documented fallback to raw section order**, and emitting `{ name, url, moodleModuleId }` using `module.url` (never `fileurl`) so no Moodle token is ever persisted in a saved link. Added two evidence-backed risks — native `mod_assign` due dates are unusable at TU Darmstadt (real exercises run through Praktomat/Artemis; a live course returned only an evaluation survey with `duedate: 0`) and the date-range heuristic can't be a hard assumption — plus one on SSO token acquisition. The "Options considered"/"Decision" sections (REST API over iCal over scraping) are unchanged.
- Updated `docs/PENDING_FEATURES.md`'s Track C section: **Phase 13 is checked off**, Phase 14 restated to the type-agnostic mapper scope above (still requiring a Vitest fixture suite, now covering both real course shapes), Phase 15 annotated with the confirmed SSO/WebView token flow, and Phase 16 rescoped from a flat preview/confirm list to a **per-week triage screen** where the user picks reading-mode or task-mode and confirms items per week.
- Gave the Moodle PoC script an optional **course-id CLI argument** (`node --env-file=.env poc.js [courseId]`) so a specific course can be targeted without editing the script. With no argument it still targets the first enrolled course; with one it selects that course and fails clearly (listing the enrolled course ids) if the id isn't among them, and rejects a non-numeric argument up front. Useful for re-running against a course that actually has active assignments (the first validation run hit an assignment-free course, so `mod_assign_get_assignments` came back empty).
- Added a throwaway **Moodle Web Services PoC** (`spikes/moodle-poc/`) so the Phase-13 approach can be validated against a real Moodle account before Phase 14 builds the core mapper. It's a standalone Node script (`poc.js`, no `package.json` / dependencies — uses Node 22's global `fetch` and `--env-file`, and is intentionally outside the `packages/*` workspaces glob) that calls `core_webservice_get_site_info`, `core_enrol_get_users_courses`, and `core_course_get_contents` (plus optional `mod_assign_get_assignments`) and dumps each raw response to a git-ignored `output/` folder. Ships with a `.env.example`, a `README.md` (how to run it, how to obtain a "Mobile app" token via `login/token.php`, and token-security cautions), and a new `.gitignore` rule for `spikes/moodle-poc/output/`.
- Added a **mobile conflict-resolution dialog** (Phase 12.2) so the write-conflict detection from 12.1 no longer surfaces as a silent overwrite or a swallowed `console.warn`. Every semester save in the app — including the fire-and-forget automatic ones (tap-to-cycle a tag in course detail, course reorder on the semester screen, the live tag editor) and the awaited add/edit form saves and file imports — now routes through one conflict-aware helper, `src/sync/saveWithConflict.ts`. On a `ConflictError` it raises a bottom-sheet dialog ("Changed on another device") offering **Keep my changes**, **Use the latest**, or **Cancel**: *keep* stashes the other device's version as a recoverable `"<name> (conflict backup)"` semester (a fresh non-colliding id) then re-reads to refresh the adapter's baseline and forces the local write through; *use the latest* swaps the on-screen semester to the cloud version (via an `onApplied` callback) and drops the unsaved edit; *cancel* leaves the unsaved edit on screen and, on the add/edit forms, keeps the user on the form so their input isn't lost. Non-conflicting single-device use is unchanged (no false positives — the baseline refreshes after each successful save).
- Wired the dialog into the app shell: a new `src/sync/ConflictProvider.tsx` (a context mounted inside the provider stack in `app/_layout.tsx`) registers a module-level opener so the non-React `saveWithConflict` helper can raise the React dialog and await the user's choice, and `src/sync/ConflictDialog.tsx` (a `<Modal>` mirroring `TagPickerSheet`'s fade-backdrop/slide-up styling) floats over any screen. No new dependencies. Ticked the conflict-resolution item in `docs/PENDING_FEATURES.md`.
- Added the **desktop conflict-resolution dialog** (Phase 12.3), the macOS/Windows counterpart to 12.2, so the cloud write-conflict `ConflictError` from 12.1 no longer overwrites another device's changes silently. A single conflict-aware save wrapper (`saveWithConflict` in `packages/desktop/app.js`) now backs all four renderer save paths — the debounced autosave, the create/edit semester form, and the file import. On a `CONFLICT` it opens a new `#conflict-overlay` modal ("Changed on another device", reusing existing modal markup/CSS — no new CSS vars) offering **Keep my changes**, **Use the latest**, or **Cancel**: *keep* stashes the other device's version as a recoverable `"<name> (conflict backup)"` semester (a fresh `<id>-conflict-backup` id) then re-loads to refresh the adapter's baseline and forces the write; *use the latest* reloads the cloud version into state and re-renders, dropping the unsaved edit; *cancel* leaves the edit on screen (autosave keeps the Unsaved indicator; the create/edit form stays open). Single-device use shows no dialog and offline (fs fallback) saves never reach it.

- Added **cloud write-conflict detection** (Phase 12.1), the foundation for the resolution dialogs in 12.2 (mobile) / 12.3 (desktop). A new pure, dependency-free core module `@lectio/core/storage/conflict` (`packages/core/src/storage/conflict.js`, dual-mode like `planner-core.js`/`migrate.js`: `window.PlannerConflict` in the renderer, `module.exports` in Node) exports `detectConflict(expected, actual)` — true only when both timestamps are present and differ (no baseline or a missing remote row → no conflict) — and a typed `ConflictError` carrying `semesterId`, `expectedUpdatedAt`, `actualUpdatedAt`, `remote` (the remote blob, for a "load latest"/backup path), and `code: 'CONFLICT'`. Unit-tested in `tests/unit/conflict.test.js`; exposed via a new `./storage/conflict` package export, a mobile ambient type + tsconfig `paths` entry.
- Wired `updated_at` tracking into **both Supabase adapters** (mobile `supabase-storage.ts` + desktop `supabase-storage.js`, kept byte-symmetric). Each adapter keeps an in-memory `id → lastKnownUpdatedAt` map filled on `list()`/`get()` (their selects now include `updated_at`) and checked on `save()`: before upserting it reads the current row's `updated_at`, and if it moved since we last observed it, throws `ConflictError` (with the migrated remote blob) instead of overwriting. This covers **every** save — including the mobile's automatic tap-to-cycle writes — without touching any call site. Cost is one extra read per save (acceptable for correctness; a future conditional-update could fold it in). Non-conflict saves are unchanged and still stamp a fresh `updated_at` on every successful write. The desktop renderer loads a vendored `conflict.js` (added to `scripts/sync-core.js` + `.gitignore` + `index.html`, before `supabase-storage.js`). The local `fs-storage`/`device-storage` adapters are intentionally untouched — a single-device local store can't conflict with itself. Both adapters' contract suites still pass the six base cases and gain a conflict + no-conflict case (the fake Supabase clients now round-trip `updated_at` with a `__setUpdatedAt` helper to simulate another device).
- Added account methods to the desktop renderer's auth layer (Phase 11.4, `auth.js`), mirroring the mobile `AuthProvider`/`account.ts`: `updateEmail(newEmail)` and `updatePassword(newPassword)` (both via `supabase.auth.updateUser`) and `deleteAccount()` — which calls the **same `delete-account` Supabase Edge Function** the mobile app uses (the anon key can't delete its own auth user; the function holds the `service_role` key in its own environment, verifies the caller's JWT, deletes the user, and the user's semesters cascade away) and then signs out locally. No `service_role` key ships in the app.
- Added a **Profile sub-window to desktop Settings** (Phase 11.4), mirroring the mobile Settings → Profile navigation: Settings shows an **Account → Profile** row with the signed-in email as a subtitle that opens a dedicated Profile window (closing it returns to Settings), which owns **change email** (sends a Supabase confirmation; the change applies after the user confirms), **change password** (new + confirm with length ≥ 6 and match validation), **Sign out**, and a de-emphasized **Delete account** (strong `confirm()` → the Edge Function → the sign-in overlay returns). Each action shows a busy state and an inline status/error line via `friendlyAuthError`; listeners are re-attached per open with the existing clone-node trick. This **formalizes sign-out in Settings and removes the temporary header "Sign out" button** added in 11.1. Existing CSS classes/variables only — no new CSS. Ticked the desktop auth/account item in `docs/PENDING_FEATURES.md`.
- Added a one-time, idempotent, **non-destructive local→cloud upload** for the desktop (Phase 11.3, `packages/desktop/local-import.js`). The first time an account signs in with semesters in local `fs`-storage, the app offers to upload copies to the cloud so they sync to other devices — local copies are never deleted. The logic diffs local vs cloud by id: a local id not in the cloud uploads as-is ("New"); one that already exists defaults to **Skip** and is never overwritten ("Already in your account"), with an optional "Upload as a copy" that re-slugs to a fresh unique id (mirroring the mobile `uniqueSemesterId`) so nothing is lost. Re-running finds everything present → all skipped; a plain upload also guards against overwriting an id that appeared meanwhile. It's a dual-mode file (`window.LocalImport` in the renderer, `module.exports` in Node) with `planUpload`/`runUpload` taking the cloud adapter by argument.
- Wired the upload offer into first sign-in (Phase 11.3, `app.js` + `index.html`). After a successful sign-in `maybeOfferLocalUpload(session)` runs: if the per-user flag `localUploadDone:<userId>` (localStorage, keyed by Supabase user id so a different account on the same machine still gets its own offer) is unset and there are local semesters, a confirm modal lists each one with a **New** / **Already in your account** badge (existing ids get a Skip / Upload-as-a-copy choice), and **Upload** / **Not now** actions. Upload reports "Uploaded N, skipped M", refreshes the semester selector, and sets the flag so it won't reappear; an empty local store sets the flag silently; **Not now** defers without setting it (re-offered later / re-openable from Settings → Profile in 11.4). `main.js` / `fs`-storage are read-only here (local data is kept as the offline fallback).
- Added a **desktop Supabase storage adapter** (Phase 11.2) — `packages/desktop/supabase-storage.js`, a vanilla-JS mirror of the mobile `supabase-storage.ts` satisfying `@lectio/core`'s async storage contract (same `[a-zA-Z0-9_-]+` id guard, same `Invalid semester id`/`Semester not found`/`Not authenticated` messages, same migrate-on-`get`, same `semesters(id, user_id, data, updated_at)` table). It's a dual-mode file like `planner-core.js` (attaches `window.createSupabaseStorage`/`window.lectioSupabaseStorage` in the renderer, `module.exports` in Node) and `createSupabaseStorage(client)` takes the Supabase client by **injection** so it runs in Node without UMD/window globals. To give the renderer the same migration the other adapters use, `@lectio/core`'s `storage/migrate.js` was made dual-mode (`window.PlannerMigrate`, logic unchanged) and `scripts/sync-core.js` now vendors it next to `index.html` alongside `planner-core.js` (git-ignored).
- Ran the desktop Supabase adapter against the **shared storage-contract suite** (`packages/core/tests/contract/desktop-supabase-storage.test.js`, the same `storage-contract.js` `fs-storage` uses): all six contract cases pass unchanged, plus an extra check that `save()` while unauthenticated rejects. The adapter is driven by an in-memory fake client (`fake-supabase-client.js`, a JS port of the mobile test's fake) injected via `createSupabaseStorage(fakeClient)`, so no real project is needed in CI.
- Switched the desktop storage layer to **Supabase with a local-`fs` offline fallback** (Phase 11.2). The renderer `api` (`list`/`load`/`save`/`remove`, signatures unchanged) now delegates to a `getActiveStorage()` that returns the Supabase adapter when there's a session and the local `fs` store (the pre-11 `window.planner.*` IPC path) when signed out. If a signed-in Supabase call throws a **connectivity** error (`isConnectivityError`, mirrored from mobile), a non-fatal "Working offline — changes may not sync" banner appears and the op falls back to the local `fs` store — best-effort and lossless for both reads and writes — so the app never crashes; genuine errors (not-found / invalid id) still propagate. Sign-out reverts to `fs` and clears the banner. This is the minimal "no crash, clear state" path — full offline↔cloud merge (and importing existing local `fs` semesters into the cloud) is deferred to later phases; a freshly signed-in account therefore starts from its (empty) cloud data. `main.js` / `fs-storage` and the mobile adapters are untouched, so synced JSON stays byte-identical across desktop and mobile.
- Added a Supabase client and auth helpers to the **desktop** renderer (Phase 11.1), the groundwork for cross-device sync. supabase-js is browser code, so the client lives in the renderer (not the Electron main process): a new `packages/desktop/supabase-client.js` builds `window.lectioSupabase` with `persistSession`/`autoRefreshToken` (the renderer has `localStorage`, so sessions survive relaunches) and `detectSessionInUrl: false`, and `packages/desktop/auth.js` exposes a thin vanilla auth surface (`getSession`/`onAuthChange`/`signIn`/`signUp`/`signOut` + a `friendlyAuthError` mapper kept identical to the mobile app's `auth-errors.ts`). Because the renderer loads plain `<script>`s (no bundler) and can't `require()` or read `process.env`, the supabase-js UMD bundle and a small `window.LECTIO_SUPABASE` config are vendored/generated next to `index.html` by a new `scripts/sync-supabase.js` (run on prestart/predev/prebuild, mirroring `sync-core.js`); both outputs are git-ignored. The URL + **publishable (anon)** key are read from the environment (`EXPO_PUBLIC_SUPABASE_*`) or, as a fallback, the mobile app's git-ignored `.env` — so they're never committed (a committed `supabase-config.example.js` documents the shape, and no `service_role` key is involved). `@supabase/supabase-js` is added as a desktop devDependency (build-time only; the renderer uses the vendored bundle).
- Gated the desktop app behind a Supabase sign-in screen (Phase 11.1). On launch the renderer now checks for a session: with none, a new sign-in overlay (email/password, "Sign in" / "Create account", busy + friendly-error states) blocks the planner; with a session, the app initialises exactly as before — **storage is unchanged and still reads the local `fs` semesters** (the storage switch to Supabase is Phase 11.2). A temporary "Sign out" button in the header clears the in-memory planner and returns to the overlay (Phase 11.4 will fold this into Settings); signing back in re-loads the local data. The account is the **same Supabase project as the mobile app**, so a desktop sign-in matches the mobile login, though data isn't shared yet.
- Fixed and extended CI for the mobile work (`.github/workflows/ci.yml`): added `dev` to the `push`/`pull_request` branch filters so PRs into the now-current integration branch are gated (previously CI only ran on `main`/`mobile-prep`), and added a lightweight Ubuntu-only `Mobile (typecheck)` job that runs `tsc --noEmit` for `@lectio/mobile`. The mobile Vitest suite is already exercised by the existing `Test` matrix via the root `npm test`, so the new job only adds the typecheck gate; the EAS build check stays deferred to Phase 18. `workflow_call` (and therefore `release.yml`'s dependency on this workflow) is unchanged. Ticked the mobile tests + CI items in `docs/PENDING_FEATURES.md`.
- Ran the mobile `supabase-storage` adapter (the one the app actually uses) against `@lectio/core`'s shared storage-contract suite. Since it can't reach a real project in CI, `test/mocks/supabase-client.ts` is an in-memory fake of `src/supabase/client.ts` emulating only the query surface the adapter calls — `from('semesters').select('id, data')` (bare-await list), `.select('data').eq('id', id).maybeSingle()` (get), `.upsert(...)`, `.delete().eq('id', id)`, and `auth.getUser()` — and `test/supabase-storage.test.ts` runs all six contract cases against it plus one extra check that `save()` while unauthenticated rejects. The fake is wired in via `vi.mock('../src/supabase/client', …)` in the test (a config alias by absolute path can't intercept the adapter's relative `../supabase/client` import); the adapter passes unchanged.
- Set up Vitest for the `@lectio/mobile` package (no Jest / react-native preset): a Node-environment `vitest.config.ts` that aliases `@react-native-async-storage/async-storage` to a tiny in-memory mock (`test/mocks/async-storage.ts`, implementing only the `getAllKeys`/`multiGet`/`getItem`/`setItem`/`removeItem` the adapter uses, plus a `__reset()`), a `test`/`test:watch` script pair, and `vitest` as the lone new devDependency. The root `npm test` now runs core's suite **and** the mobile suite, so CI exercises both.
- Ran the mobile `device-storage` adapter against `@lectio/core`'s shared storage-contract suite (`test/device-storage.test.ts` imports the same `storage-contract.js` core's own `fs-storage` test uses): all six contract cases pass — empty list, save/get with default-tag migration, legacy status→tag-id migration, delete, missing→reject, and traversal-id→reject — confirming the adapter satisfies the contract unchanged.
- Turned the mobile Profile screen into a full account hub (`app/profile.tsx`): alongside the signed-in email and sign-out, it now offers **change email** (calls `updateEmail`; Supabase emails a confirmation to the new address and the change applies after the user confirms), **change password** (an inline new-password + confirm form validating length ≥ 6 and that both match, via `updatePassword`), and a red **Delete account** action behind a strong destructive confirm. Each action uses busy state and `friendlyAuthError`. Settings (`app/settings.tsx`) no longer duplicates the email card and sign-out button — it now has an **Account** row showing the email as a subtitle that links to the Profile hub, keeping the About rows (Send feedback, Start tutorial) and version line.
- Added account deletion via a Supabase **Edge Function** (`supabase/functions/delete-account/index.ts`, Deno): a client with the anon key can't delete its own auth user, so the app's `src/auth/account.ts` `deleteAccount()` invokes the function (which forwards the user's JWT), and the function verifies the token with a service-role client and calls `admin.deleteUser` — the user's semesters cascade away via the table FK. The service_role key lives only in the function's environment (never committed or shipped). On success the client signs out locally; `AuthProvider` exposes `deleteAccount` as a thin pass-through. Requires a one-time `supabase functions deploy delete-account` (see the PR); without it, Delete shows a clean error rather than crashing.
- Made the mobile app resilient to a paused free-tier Supabase project or an offline device. `AuthProvider` now wraps the initial `getSession()` in try/catch with a `connectionError` flag and a `retryConnection()` helper, so a paused/unreachable project no longer spins forever: at launch (with no cached session) `app/_layout.tsx` shows a "Can't reach Lectio's servers" view with a Retry button instead of an endless spinner, while a cached session still lets the app in (offline-tolerant) and lets individual data calls fail softly. The sign-in screen detects connectivity errors (`isConnectivityError`) and shows a clear "server may be paused / you're offline" message. No new app dependencies (the Edge Function is Deno, separate from the RN bundle).
- Added Google/Apple sign-in to the mobile app, built now but prepared for the Phase-18 dev build. A browser-based OAuth helper (`src/auth/oauth.ts`) runs Supabase `signInWithOAuth` with `skipBrowserRedirect`, opens the provider page via `expo-web-browser`'s `openAuthSessionAsync` against a `lectio://` deep link (`expo-linking` `createURL('/')`), and defensively exchanges whatever the redirect returns for a session — `?code=` via `exchangeCodeForSession` (PKCE) or `#access_token`/`refresh_token` via `setSession` (implicit). An `isExpoGo` check (`src/auth/env.ts`, `Constants.executionEnvironment === 'storeClient'`) guards both providers: the `AuthProvider` `signInWithGoogle`/`signInWithApple` stubs now call the helper, but in Expo Go they throw a clear "needs the installed app" message instead of failing on the unreliable redirect round-trip. The sign-in screen gains a subtle "or" divider and "Continue with Google" / "Continue with Apple" buttons (Apple iOS-only per guideline), each surfacing `friendlyAuthError`; email/password, forgot-password and sign-out are unchanged. Two new SDK-56-pinned deps (`expo-web-browser ~56.0.5`, `expo-apple-authentication ~56.0.4`); `app.json` registers both config plugins (the Apple plugin adds the Sign-in-with-Apple entitlement on the dev build). OAuth only completes a real session once the Supabase/Google/Apple console steps are done (see the PR description) — not testable in Expo Go.
- Added a mobile "Forgot password?" flow: a new `app/forgot-password.tsx` screen (mirroring the sign-in card style, with its own header for a back affordance) takes an email and calls `resetPassword`, then switches to a privacy-safe confirmation state — "If an account exists for <email>, a reset link is on its way" shown identically whether or not the account exists — plus a "Back to sign in" action. Registered in `app/_layout.tsx`. Tapping the emailed link completes the reset in a browser; a full in-app deep-link completion needs a dev build (Phase 18), so for now the screen only sends the email.
- Surfaced friendlier auth feedback on the sign-in screen (`app/sign-in.tsx`): the catch now renders `friendlyAuthError(...)` instead of the raw Supabase string (e.g. "Wrong email or password." for bad credentials), a small "Forgot password?" link sits below the password field and pushes `/forgot-password`, and the latent email-confirmation seam is wired — when `lastSignUpNeedsConfirmation` is true (only once confirmation is enabled in the Supabase console) an inline "Check your inbox to confirm your email, then sign in." notice with a "Resend email" action appears. With confirmation OFF (today's default) the notice stays dormant and account creation still signs in immediately — no behavior change. No new dependencies.
- Added a friendly auth-error mapper (`packages/mobile/src/auth/auth-errors.ts`): `friendlyAuthError(err)` turns Supabase's raw messages/status codes into short, human sentences (wrong credentials, unconfirmed email, already-registered, weak/short password, invalid email, rate-limit/429, signups-disabled, and unreachable-server), and `isConnectivityError(err)` flags network/paused/timeout cases so the UI can show a retry state. Pure helpers with no supabase-js type dependency beyond `unknown`; not yet wired into screens (later prompts consume them).
- Extended the mobile `AuthProvider` with the auth plumbing the rest of Phase 8 builds on, without changing any existing behavior: a `lastSignUpNeedsConfirmation` flag that `signUp` sets when Supabase returns no session + a user (the latent email-confirmation seam — stays false while confirmation is off); `resetPassword(email)` (sends the reset email with an `expo-linking` `createURL('/sign-in')` redirect); `resendConfirmation(email)`; `updateEmail`/`updatePassword` (via `supabase.auth.updateUser`); and throwing `signInWithGoogle`/`signInWithApple` stubs (8.2 fills in the OAuth bodies). The existing `signIn`/`signUp`/`signOut` signatures and throwing behavior are unchanged, and no new dependencies were added (`expo-linking` was already present).
- Added a mobile file-transfer module (`src/lib/transfer.ts`) wrapping the platform file I/O around the shared `@lectio/core` envelope helpers: `exportSemester`/`exportCourse` write a pretty-printed `.lectio.json` to the cache (via SDK 56's `expo-file-system` `File`/`Paths` API) and present the system share sheet (`expo-sharing`); `pickSemesterFile`/`pickCourseFile` open the document picker (`expo-document-picker`, no mime filter so a `.lectio.json` is never greyed out — content is validated instead), read and JSON-parse the file, and validate it with core's `parseSemesterFile`/`parseCourseFile`; `saveImportedSemester` applies the Keep/Reset-progress choice (`withResetStatuses`) and **never overwrites** — a colliding id is re-slugged to a fresh unique id (`uniqueSemesterId`) before saving; `saveImportedCourse` adds the course with brand-new ids (`prepareImportedCourse` + core's `uid`). The bytes written/read are the exact desktop envelope, so files interchange between desktop and mobile. Three new Expo deps, pinned by `expo install` to SDK-56 versions (`expo-file-system`, `expo-sharing`, `expo-document-picker`); `expo install` also registered the `expo-sharing` config plugin in `app.json`.
- Wired import/export into the mobile UI, with **export on the top bar** and **import in the "+" add-sheet**. Export is a single header action drawn as a box-with-an-up-arrow glyph (`src/components/ExportIcon.tsx`, no icon library, no emoji): on the courses screen (`app/semester/[id].tsx`) it exports the whole semester, and on the course detail screen (`app/semester/[id]/course/[courseId].tsx`) it exports just that course. Import lives entirely in the unified "+" sheet (`app/add.tsx`): the Semester tab ends with "Import a semester from a file" (picks a file, then asks Keep vs Reset progress; a colliding id imports as a new id, never overwriting), and the Course tab ends with "Import a course from a file" that adds the picked course to the selected semester with fresh ids. Both import flows dismiss the sheet on success and the underlying screen refreshes on focus; failures surface via an `Alert`. Ticked **Import / export** in `docs/PENDING_FEATURES.md`.

- Added pure, platform-free `.lectio.json` import/export helpers to `@lectio/core` (`src/integrations/lectio-file.js`, exported as `@lectio/core/integrations/lectio-file`) so the mobile app can produce and consume files byte-compatible with the desktop's. They re-implement the exact envelope contract — `{ _lectioType: 'semester'|'course', _version: 1, … }` — that the desktop builds in `ipc-handlers.js` and validates in `app.js`: `buildSemesterFile`/`buildCourseFile` (the latter applying the desktop's "clean" course projection), `parseSemesterFile`/`parseCourseFile` (same validation and user-facing error messages), and the import transforms `withResetStatuses` (reset every item to its default pending tag), `slugify` + `uniqueSemesterId` (the desktop's slug + collision loop, re-implemented locally so core never depends on the apps), and `prepareImportedCourse` (assign fresh ids via core's `uid`, or an injected id maker). No file I/O, Electron, or network — only build/parse/transform logic; the platform share-sheet/document-picker wiring stays in the apps. The desktop's `ipc-handlers.js`/`app.js` are untouched and still own its path. Covered by `tests/unit/lectio-file.test.js` and mirrored in the mobile ambient types (`lectio-core.d.ts` + a tsconfig `paths` entry for the new subpath).

- Added a first-run onboarding tour to the mobile app (`src/tutorial/`): a paged, full-screen walkthrough that mirrors the desktop's replayable tour without its DOM spotlight (RN has no DOM). `steps.ts` holds nine concise cards adapted from the desktop's `TUTORIAL_STEPS` and reworded for mobile navigation (welcome, semesters list + "+", courses + progress bars, the tag picker, Study Mode, Sort, Breakdown, Settings/gear, and a closing "you're all set"); `TutorialProvider.tsx` exposes `{ active, index, total, start, next, back, skip, finish }`, where finishing or skipping sets `prefs.setTutorialSeen(true)`; `TutorialOverlay.tsx` renders an RN core `<Modal transparent>` with a dimmed backdrop and a centered themed card showing the step's title/description, a "Step n of total" counter, and Back (hidden on the first step) / Skip / Next (Done on the last). No new dependencies.
- Wired the tour into the app (`app/_layout.tsx`): `TutorialProvider` wraps the app alongside the other providers and `TutorialOverlay` renders once near the root so it floats above every screen. A once-per-launch effect, guarded to run only when a session exists (so it never shows over sign-in), reads `prefs.getTutorialSeen()` after auth and calls `start()` when it's false. Added a "Start tutorial" row to the Settings hub's About section (`app/settings.tsx`) that replays the tour on demand and returns to the semesters list so the overlay floats over the main UI — replaying does not clear the seen flag, so first-run still shows only once. Ticked "Onboarding tour" in `docs/PENDING_FEATURES.md`.
- Added a mobile feedback client (`src/lib/feedback.ts`) that posts to the same Vercel endpoint the desktop uses (`https://lectio-opal.vercel.app/api/feedback`, which files a GitHub issue on our behalf) — same request body shape `{ type, title, body, version }` and same `{ ok, url }` response, so mobile submissions land as issues identical to desktop ones (correct `bug`/`enhancement` label, "Lectio v<version>" footer). The version is read from `expo.version` via the already-present `expo-constants`; no GitHub account is needed by the user and no secrets ship in the app (the token lives only in the Vercel function). No new dependencies.
- Added an in-app Feedback screen (`app/feedback.tsx`, registered in `_layout.tsx`) reachable from a new "Send feedback" row in the Settings hub's About section. It mirrors the desktop feedback modal: a Bug | Feature toggle (reusing `FormTabs`), a title and description with required-field validation, a sending state, an inline error on failure (no crash), and a success confirmation. Ticked the "In-app feedback" item in `docs/PENDING_FEATURES.md`.
- Added a mobile Settings hub (`app/settings.tsx`, registered in `_layout.tsx`): the Semesters header now opens it via a drawn gear in the top-left (an 8-tooth star composed from two rotated accent `View` squares with a center hole — no emoji, no icon library), replacing the old drawn-person "Profile" button. Settings opens directly onto a Profile section showing the signed-in email and the sign-out action (the former `app/profile.tsx` content moved inline), structured so the upcoming Feedback section and "Start tutorial" entry can slot in. `app/profile.tsx` stays registered for now. Ticked the "Settings screen" item in `docs/PENDING_FEATURES.md`. No new dependencies.
- Added a readings-vs-tasks breakdown to the mobile courses screen (`src/components/CourseBreakdown.tsx`), mirroring the desktop's Breakdown panel: per course, two mini progress lines — Readings done/total and Tasks done/total — computed by core's `courseBreakdown` (so the numbers match the desktop for the same semester) and reusing the existing `ProgressBar` for the bars. It sits behind a per-screen header toggle — a `list.bullet.indent`-style bullet-list glyph drawn from plain Views (no icon library), accent when on and muted when off, beside Sort/Edit — that defaults off so the list stays clean, mirroring the desktop's `state.breakdownOpen`. The counts honor Study Mode when it's on (narrowing to Studied items via the same flag). No new dependencies.
- Relabeled the course-sort menu on both desktop and mobile to five plain-language options in a new order — Weeks: high to low (`week-desc`), Weeks: low to high (`week-asc`), Progress: high % first (`progress-desc`), Progress: low % first (`progress-asc`), A-Z (`alpha-asc`) — and dropped the sixth "Z → A" order (`alpha-desc`) from the product entirely (core `SORT_ORDERS` + `sortedCourses`, the desktop `#sort-select` / `restoreSort` / `sortedCourses`, the mobile `SortMenu` options and `SortOrder` type). The five surviving string values are unchanged, so the semester JSON and desktop↔mobile parity are untouched. Any saved `alpha-desc` preference degrades gracefully: core/desktop sorting falls through to A → Z for unknown orders, the desktop's `restoreSort` lands a stale value on `progress-desc`, and the mobile `useSortOrder` hook now defaults to `progress-desc` and rewrites a no-longer-valid saved value so it doesn't linger.
- Added sort controls to the mobile courses and course-detail screens: a plain ↑↓ header action (`SortButton`, accent-colored text sitting beside the Edit action, styled like it) opens a bottom sheet (`src/components/SortMenu.tsx`, plain RN `<Modal>`, no new dependencies) offering the same six orders as the desktop's header dropdown with the same wording — Progress ↓/↑, A → Z, Z → A, Week ↑/↓ — with the active order highlighted. The courses list is fed through core's `sortedCourses` (week orders fall back to A → Z for course ordering, exactly as on desktop), and progress sorts honor Study Mode via the same `courseProgress` flag; on the readings/tasks screen the week orders sort the items by their week (ascending/descending), while the other orders leave item order unchanged. Sorting is display-only — both paths return new arrays, so the semester JSON's course and item order is never modified. The choice is shared between the two screens and persists across relaunches via a new `useSortOrder` hook (`src/lib/use-sort-order.ts`) over the existing `prefs.getSortOrder`/`setSortOrder`, re-read on screen focus and validated against core's `SORT_ORDERS` (unknown values fall back to A → Z).
- Added Study Mode to the mobile app: a bottom-left floating toggle on the courses screen (`src/components/StudyFab.tsx`, mirroring the bottom-right "+" Fab) showing a graduation cap drawn from plain Views — colorless (surface + muted cap) when off, accent-filled when on — plus a small "Study Mode — counting only Studied items" banner above the list while active. When on, the course-list progress bars and the course-detail percentage count only items tagged Studied, by passing the flag into core's `courseProgress` — same calculation the desktop uses. The state lives in a new `StudyModeProvider` context (`src/study/StudyModeProvider.tsx`, wrapped around the app in `_layout.tsx`) backed by the existing `prefs.getStudyMode`/`setStudyMode`, so it survives relaunches. Study Mode is a pure calculation overlay: it never changes tags or the semester JSON. No new dependencies.
- Replaced the mobile course-detail tap-to-cycle interaction with a tag picker: tapping a reading/task now opens a bottom sheet (new `src/components/TagPickerSheet.tsx`, plain RN `<Modal>`, no new dependencies) listing the type's tags grouped into Pending and Done sections — mirroring the desktop's tag dropdown — with the item's current tag highlighted (accent color + trailing ✓). Picking a tag writes the exact same `item.status` tag id the desktop writes, via core's `setItemStatus`, so the semester JSON stays identical; tapping the dimmed backdrop dismisses without changes. The old `cycleTag` handler was removed (the picker fully replaces blind cycling), the summary hint now reads "Tap an item to set its tag.", and long-press edit/delete, swipe actions, and batch edit mode are unchanged.
- Extracted the bodies of the three mobile add/edit forms into reusable components (`src/add/SemesterFields.tsx`, `CourseFields.tsx`, `ItemFields.tsx`): fields + save logic moved verbatim, taking ids/mode as props instead of route params. The original `semester-form` / `course-form` / `item-form` screens are now thin wrappers (SheetHeader + body) and remain the edit entry points; the item form keeps its Reading | Task switch in create mode only.
- Added a unified "+" add-sheet (`app/add.tsx`, modal at `/add?context=semester|course|tags`) for the container objects, with all three tabs — Semester · Course · Tags — selectable from every screen; `context` only picks the initial tab (Semesters FAB → Semester, Courses FAB → Course, Course-detail FAB → Tags). The Course and Tags tabs carry a semester picker (pill row from `storage.list()`) that defaults to the semester the sheet was opened from, else the first one, with a "create a semester first" hint when none exist. The Tags tab embeds the tag editor directly for the picked semester (keyed by semester id so switching the pick reloads it). The "+" never creates individual readings/tasks — those keep being added from the per-section "+ Add" controls next to the Readings/Tasks headers.
- Header consistency on mobile: the Semesters screen now shows Profile top-left as a drawn person glyph (two accent-colored shapes — no emoji, no icon library) and Edit/Done top-right with the batch Delete (n) control, matching the Courses and Course-detail screens where Edit already sits top-right. The "Tags" header button on the Courses screen was removed — the tag editor now lives in the "+" sheet's Tags tab.
- Added a per-semester tag editor to the mobile app (`src/add/TagsFields.tsx`, embedded in the "+" add-sheet's Tags tab): Readings/Tasks tabs, each grouped into Pending and Done sections like the desktop's Tags tab. Tags can be added (inline name + swatch form per section), renamed (tap the name, commits on blur), recolored (tap the color dot to open the same fixed 8-color palette as the course form), deleted (behind a confirmation alert; items wearing the tag become ghosts via core, so progress stays stable), and reordered (↑/↓ controls, swap-based within the tag's section). Protected tags (pending/studied) render a static name and can only be recolored — no rename, delete, or reorder controls — mirroring the desktop. All mutations go through the shared `@lectio/core` tag functions (`addTag`/`editTag`/`deleteTag`/`reorderTags`, now declared in the mobile ambient types), so the semester JSON stays byte-compatible with the desktop. No new dependencies.
- Added an in-sheet header to the three mobile add/edit modal sheets (semester, course, reading/task forms) via a new `SheetHeader` component (`src/components/SheetHeader.tsx`, no new dependencies): a circular ✕ close button at the top left (dismisses via `router.back()`, with an accessibility label and pressed feedback) plus the centered sheet title. The native stack header is hidden on these three modal screens so the header sits at the very top of the sheet; on Android the component pads itself below the status bar via the safe-area inset. A grabber-bar-only first iteration was replaced because Android modals aren't natively drag-dismissable — the ✕ gives both platforms an explicit close affordance, while iOS sheets additionally keep their native drag-to-dismiss.
- Added a calendar date entry to the mobile forms via a new reusable `DateField` component (`src/components/DateField.tsx`) backed by `@react-native-community/datetimepicker` (the one new dependency, pinned by `expo install` to the SDK 56 version, Expo Go compatible). A pressable field styled like the existing inputs opens the native picker — a one-shot dialog on Android, an inline picker in a bottom-sheet modal with a Done button on iOS. Dates are formatted with a local-time `toYMD` helper (no `toISOString`, so no off-by-one in negative-offset timezones); the on-disk format stays the exact `YYYY-MM-DD` string the desktop writes.
- The semester form's start date and the item form's task due date now use `DateField` instead of typed `YYYY-MM-DD` TextInputs. The due date keeps its Clear control (now built into `DateField` via `allowClear`, round-tripping to `''` = no due date), and both forms keep their `isValidDate` submit-time guards as a safety net.
- Added a device-local UI preferences module to `@lectio/mobile` (`src/lib/prefs.ts`): a tiny typed AsyncStorage wrapper (keys namespaced `lectio:pref:`) for last-opened semester, Study Mode, sort order, and tutorial-seen — mirroring the desktop's `readPref`/`writePref` semantics (reads never throw, writes fail silently). This is UI state only and never touches the semester JSON or Supabase rows.
- First prefs consumer: the mobile app now reopens the last-opened semester on launch. The semester screen records its id on focus, and the semesters list auto-navigates to it once per app launch (a module-level guard prevents re-jumping when the user deliberately navigates back, and the jump is skipped in batch edit mode). If the recorded semester was deleted, the stale pref is cleared and the list is shown.
- Moved three more pure planner helpers into `@lectio/core/planner-core` (foundation for upcoming mobile features; the desktop keeps its inline copies for now, zero behavior change): `courseBreakdown` (per-course done/total split by readings/tasks, honoring Study Mode and ghost items), `sortedCourses` + the `SORT_ORDERS` list (non-mutating course sorting by progress/alphabetical, with week sorts falling back to A → Z, mirroring the desktop), and `setItemStatus` (assign a tag id directly to a reading/task, clearing any ghost marker). All three are mirrored in the mobile ambient types (`lectio-core.d.ts`) and covered by new `breakdown.test.js` / `sort.test.js` unit suites.
- The new suites also exercise the helpers through core's CommonJS surface (via a small `tests/helpers/require-core.cjs` loader) — the same `require()` path the desktop main process uses — which additionally keeps the v8 coverage accounting for the dual-loaded `planner-core.js` above the enforced thresholds.
- Added a "Reading / Task" tab to the desktop's New/Edit modal (the "+ New" button's dialog), mirroring the mobile add flow: a Reading | Task toggle (Task reveals an optional due-date field), a course selector, week (clamped to the semester's week count) and title; submitting with "Add" appends the item to the currently open semester via core's `addItem` — the same shape as the inline add-rows — then persists and re-renders. With no open semester or no courses the tab shows a hint and disables the submit. The hidden semester fields drop their `required` flags while this tab is active so the form can submit.
- Unified the mobile add flows behind a shared, permanent bottom-right "+" floating action button (`src/components/Fab.tsx`) on the Semesters, Courses, and Course-detail screens — it stays fixed while the content scrolls (lists keep enough bottom padding for the last row to clear it), replacing the previous per-screen "+ New" / "+ Course" pill buttons and adding the missing FAB on course detail.
- Added an add-type tab row (`src/components/FormTabs.tsx`) to the create forms, indicating what is being added: "Semester" on the semester form, "Course" on the course form, and switchable "Reading | Task" tabs on the item form (Reading active by default; selecting Task reveals the due-date field). Edit mode is unchanged and shows no tabs; all field logic and save paths stay in the existing form screens — nothing was duplicated.
- Added semester CRUD to the mobile app: a new modal form screen (`app/semester-form.tsx`) creates and edits semesters (name, start date, weeks — validated, no new dependencies, Expo Go compatible), with slug-based unique ids (`src/lib/semester-id.ts`) mirroring the desktop's `slugify` + collision loop. New semesters get cloned copies of the default reading/task tag arrays; edits keep the existing id and preserve courses and tags.
- Wired the mobile semesters list (`app/index.tsx`) to the new form: a "+ New" header button opens create mode, long-pressing a row offers Edit / Delete (delete behind a confirmation alert), and the list refreshes after each mutation.
- Replaced the bare "No semesters yet." empty state with a real first-run path: "Create your first semester" opens the form, and "Add sample semester" seeds the existing sample via `ensureSeed` (which still no-ops when storage is non-empty).
- Ticked the corresponding "Content editing" items (semester CRUD, first-run path) in `docs/PENDING_FEATURES.md`.
- Added `react-native-gesture-handler` to `@lectio/mobile` (pinned tree-wide to 2.31.2 via the root `overrides`, matching Expo Go SDK 56's native module) and a reusable `SwipeableRow` component (`src/components/SwipeableRow.tsx`); the root layout is wrapped in `GestureHandlerRootView`.
- Swipe actions on mobile: swiping a row left reveals a red Delete action (semesters, courses, and readings/tasks — each behind a confirmation alert); swiping a semester row right reveals a blue Edit action that opens the semester form. The long-press Edit/Delete menu on semesters is kept.
- Batch edit mode on mobile: an Edit header button (top-left on the semesters list; top-right on the courses and course-detail screens, where the back button occupies the left slot) toggles a selection mode with per-row select circles and a "Delete (n)" header action. Course deletion goes through `@lectio/core`'s `deleteCourse`.
- Moved the "+ New" action to a large rounded floating button pinned to the bottom-right of the semesters list (hidden while batch edit mode is active), and replaced the "Sign out" header button with a "Profile" entry opening a new profile screen (`app/profile.tsx`) that shows the signed-in email and the sign-out option (only that, for now).
- Added two pure course helpers to `@lectio/core/planner-core` with unit tests: `editCourseColor` (recolor, returns the course or null) and `reorderCourses` (in-place reorder to a given id order, mirroring `reorderTags`); both are mirrored in the mobile ambient types.
- Added mobile courses CRUD: a new modal course form (`app/semester/course-form.tsx` — one static route taking `id`/`courseId` query params) creates and edits courses with a name field and a fixed 8-color swatch palette (a course's current off-palette color is kept as an extra leading swatch so editing never silently changes it). Saves follow the fresh-load → mutate-via-core → persist pattern.
- Wired the courses list (`app/semester/[id].tsx`): a floating "+ Course" button (matching the semesters screen pattern), swipe right on a course to edit, long-press for an Edit / Move up / Move down / Delete menu (reorder goes through core's `reorderCourses` and persists), and an "Add a course" button in the empty state. Swipe-left delete and batch edit mode were already in place from 7A.
- Added three pure item helpers to `@lectio/core/planner-core` with a new `items.test.js` unit suite: `addItem` (readings get `r-pending`, tasks `t-pending` + `dueDate` defaulting to `''`, matching the desktop's `addRow` shape), `editItem` (patches only provided title/week/dueDate; empty dueDate clears it, tasks only), and `deleteItem`; all three mirrored in the mobile ambient types.
- Added a mobile reading/task form (`app/semester/item-form.tsx` — static modal route taking `id`/`courseId`/`kind`/`itemId` query params): title, week (clamped to the semester's week count), and — for tasks only — an optional `YYYY-MM-DD` due date with a Clear control. Saves follow the fresh-load → mutate-via-core → persist pattern.
- Wired the course detail screen: "+ Add" buttons on the Readings/Tasks section headers (the empty texts are tappable too), long-press an item for Edit / Delete, swipe right to edit, tasks show "due YYYY-MM-DD" under their week line, and single-item deletion now goes through core's `deleteItem`. Tap-to-cycle and the 7A swipe-left delete / batch edit mode are unchanged. With this, Phase 7 is complete: a brand-new account can build a full semester (semester → courses → readings/tasks) entirely on the phone.
- Fixed the PR "Build (macOS, no publish)" CI job: the `--publish never` flag was being lost through the two npm-workspace layers (root alias → workspace script), so electron-builder ran as `--mac never` and failed with "Unknown target: never". The build now invokes the desktop workspace script directly (`npm run build:mac --workspace @lectio/desktop -- --publish never`) so the flag forwards correctly and electron-builder packages the `.dmg`/`.zip` without attempting to publish, dropping the spurious `GH_TOKEN` requirement. Applied the same single-layer fix to `release.yml` (which had the identical latent bug), keeping `--publish always` there for real tagged releases.
- Bumped the GitHub Actions in `ci.yml`/`release.yml` to majors that run on Node.js 24 (`actions/checkout@v6`, `actions/setup-node@v6`, `actions/upload-artifact@v7`), clearing the Node.js 20 deprecation warning.
- Documentation sync: brought `README.md`, `CLAUDE.md`, and the `docs/` files in line with the monorepo + desktop + mobile + Supabase reality (no longer "desktop-only"), de-duplicated the macOS signing prose into `docs/MACOS_SIGNING.md`, fixed stale root-relative paths in the icon/signing docs, and added `docs/PENDING_FEATURES.md` plus a `packages/mobile/README.md`.

### Mobile preparation — Phase 6: Supabase sync

- Added `@supabase/supabase-js` and `react-native-url-polyfill` to `@lectio/mobile`; the Supabase client (`src/supabase/client.ts`) reads `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` from env vars and throws a clear error when either is missing. Token auto-refresh is wired to `AppState` so the session refreshes when the app returns to the foreground.
- Added a Supabase storage adapter (`src/storage/supabase-storage.ts`) backed by the `public.semesters` table that satisfies the same `@lectio/core` async storage contract as the device adapter — same method names, same id guard, same "invalid"/"not found" error messages, same `migrateStatusToTagId` on load, validated by `assertStorage`. Row Level Security on the server enforces user isolation; the adapter sets `user_id` on upsert using the composite primary key `(user_id, id)`.
- Added an `AuthProvider` React context (`src/auth/AuthProvider.tsx`) that restores the persisted session via `AsyncStorage` on launch, subscribes to `onAuthStateChange`, and exposes `signIn` / `signUp` / `signOut` to the app.
- Added a sign-in screen (`app/sign-in.tsx`) with email + password fields, "Sign in" and "Create account" buttons, inline error display, and a loading state — no OAuth, no native modules, works in Expo Go.
- Wired the root layout (`app/_layout.tsx`) to show a spinner during session restore, redirect unauthenticated users to `/sign-in`, and redirect back to `/` on sign-in — using `useSegments` + `router.replace` for the Expo Router file-based navigation model.
- Switched the app's active storage from `createDeviceStorage` to `createSupabaseStorage`; `createDeviceStorage` is kept exported as the offline/local fallback for a future offline-mode phase. The first-run seed (`ensureSeed`) is no longer auto-called to avoid silently pushing sample data to every new cloud account.
- Added a "Sign out" header button to the semesters screen; on sign-out, in-memory state is cleared and the auth redirect navigates back to the sign-in screen.
- Added `packages/mobile/.env.example` with placeholder values (`EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`) and added `.env` / `.env.local` to `packages/mobile/.gitignore` (the root `.gitignore` already covers `.env*`). The Supabase adapter contract test is deferred — manual cross-device round-trip and RLS isolation are the verification criteria for this phase.

### Mobile preparation — Phase 5: mobile scaffold + MVP UI

- Scaffolded a new `@lectio/mobile` workspace package: an Expo (SDK 56, React Native 0.85, React 19.2) app using Expo Router (file-based) and TypeScript, runnable in Expo Go with no native/dev-client build. Added a monorepo-aware `metro.config.js` (watches the repo root, resolves the hoisted `@lectio/core` symlink, disables hierarchical lookup) so Metro bundles the shared core.
- Added an on-device storage adapter (`src/storage/device-storage.ts`) backed by `@react-native-async-storage/async-storage` that satisfies the Phase-4 async storage contract — same `migrateStatusToTagId` on load, same id guard (rejects ids not matching `/^[a-zA-Z0-9_-]+$/` with "invalid", missing with "not found"), validated by `assertStorage` — so it behaves identically to the desktop fs adapter.
- Added hand-written ambient types for `@lectio/core` (`types/lectio-core.d.ts`) covering the data shapes and the RN-safe subpaths (`.`, `./planner-core`, `./storage/migrate`, `./storage/contract`); tsconfig `paths` map those specifiers to the declaration file so the symlinked JS package is typed without touching core's published surface. `npx tsc --noEmit` passes. Added a first-run seed (`src/storage/seed.ts`) that saves one realistic sample semester (2 courses with varied readings/tasks) when storage is empty, including the default tag arrays so migration leaves it untouched.
- Added the MVP screens (Expo Router + TypeScript): semesters list → courses list (per-course progress bars from `courseProgress`) → course detail (readings/tasks with their tag name + color dot; tapping an item advances it to the next tag, recomputes progress via core, and persists). All planner math comes from `@lectio/core` — none is reimplemented in the app. Added a minimal light/dark theme (`src/theme.ts`) driven by the OS color scheme.
- Added root convenience scripts (`mobile`, `mobile:ios`, `mobile:android`) delegating to the `@lectio/mobile` workspace.
- The device adapter's contract test was skipped this phase (the reusable suite is Vitest-based and AsyncStorage needs RN-specific mocking that isn't worth standing up yet); it will be contract-tested in a later phase. Core and desktop are unchanged.
- Fixed a launch crash on SDK 56: flattened the `style` arrays on the `Pressable` children of `<Link asChild>` (`StyleSheet.flatten`), which the rewritten Expo Router renders through a `<Slot>` that no longer accepts an array style on its direct child.
- Fixed Android bundling: added `@expo/ui` (required by SDK 56's Expo Router for its native toolbar — `@expo/ui/jetpack-compose` on Android) and its `react-native-reanimated`/`react-native-worklets` peers, pinned tree-wide to the SDK 56 versions via a root `overrides` (`reanimated@4.3.1`, `worklets@0.8.3`) so they dedupe to a single copy and satisfy `expo-modules-core`. Verified bundling and launch on both the iOS simulator and the Android emulator.

### Mobile preparation — Phase 4: storage abstraction

- Extracted `migrateStatusToTagId` from `semester-store.js` into a new platform-agnostic `@lectio/core/storage/migrate` module (depends only on `planner-core`, no `fs`); `semester-store.js` now consumes and re-exports it, keeping its public API and behaviour identical.
- Added `@lectio/core/storage/contract`: the canonical async storage interface (`list`/`get`/`save`/`delete`) documented as a single source of truth, with a `STORAGE_METHODS` list and an `assertStorage()` runtime validator that every platform adapter is checked against.
- Added `@lectio/core/storage/fs` — a filesystem adapter (`createFsStorage(dirOrResolver)`) that satisfies the async contract by wrapping the existing synchronous `semester-store`, accepting a directory string or a `() => dir` resolver (mirroring `ipc-handlers`). Additive only: the desktop, `ipc-handlers.js`, and existing behaviour are unchanged.
- Added a reusable `tests/contract/storage-contract.js` suite (run by `tests/unit/fs-storage.test.js` against the fs adapter) that exercises the full contract — list/get/save/delete, migration on load, missing-id and traversal-id rejection — so the future Supabase adapter can run the same suite.
- Added `./storage/migrate`, `./storage/contract`, and `./storage/fs` subpath exports to `@lectio/core`.

### Mobile preparation — Phase 3: CI monorepo fixes

- Fixed the CI coverage artifact path: the "Upload coverage report" step now uploads from `packages/core/coverage/` (where Vitest writes coverage in the monorepo) instead of the stale root `coverage/`.
- Switched CI installs to `npm ci` for reproducible, lockfile-pinned dependencies, and narrowed the `ci.yml` triggers to push/PR on `main` + `mobile-prep` (keeping `workflow_call` so `release.yml` can require the workflow) instead of running on every push of every branch.
- Added a macOS-only `build-macos` job to `ci.yml` that runs the full prebuild chain (`sync-core` + `bundle-deps`) and electron-builder without `--publish`, so packaging breakage is caught on PRs rather than at tag time. It gates on the `test` job and uploads no artifacts.
- Fixed the `release.yml` "Upload build artifacts" paths (macOS and Windows) to point at `packages/desktop/dist/` (electron-builder's output in the monorepo) instead of the stale root `dist/`.

### Mobile preparation — Phase 2: relocate desktop into @lectio/desktop

- Moved the entire desktop app (`main.js`, `preload.js`, `index.html`, `app.js`, `style.css`, `start.command`, and the `assets/`, `build/`, `semesters/` dirs) from the repo root into `packages/desktop/` via `git mv` to preserve history.
- Rewired the desktop to consume `@lectio/core` directly: `main.js` now requires `@lectio/core/ipc-handlers`, and the three temporary `lib/` re-export shims from Phase 1 (plus the empty `lib/` dir) are removed.
- The sandboxed renderer can't `require()` core, so `scripts/sync-core.js` vendors `planner-core.js` next to `index.html` (on prestart/predev/prebuild, git-ignored); `index.html` loads it via `<script src="planner-core.js">`, a relative path that resolves identically under `npm start` and in the flattened packaged bundle. electron-builder bundles `@lectio/core` as a production dependency for the main process.
- Added `packages/desktop/package.json` (`@lectio/desktop`) carrying the desktop scripts, the Electron dependencies, and the electron-builder `build` block (moved out of the root); its `files` list now bundles the vendored `planner-core.js` instead of `lib/`.
- Made the packaged build work from the npm workspace: pinned `electron` to an exact version (electron-builder can't compute it from a range when the module is hoisted) and added `scripts/bundle-deps.js` (a prebuild step) that seeds `packages/desktop/node_modules` with the production-dependency closure so electron-builder bundles `@lectio/core` + `electron-log` + `electron-updater` into the app instead of running a destructive workspace install. `scripts/clean-deps.js` removes that seed on predev/prestart so dev keeps using the live workspace packages.
- Slimmed the root `package.json` to a thin workspace manager: removed the desktop scripts, the `build` block, the Electron deps, and the `main` field; added `start`/`dev`/`build:mac`/`build:win` scripts that delegate to the `@lectio/desktop` workspace.
- Updated `README.md` and `CLAUDE.md` for the `packages/core` + `packages/desktop` layout (project tree, command paths, data/dev folder locations); `api/`, `scripts/`, `macos-signing/`, and `homebrew/` remain at the repo root.

### Mobile preparation — Phase 1: extract @lectio/core

- Moved the three dual-mode core modules (`planner-core.js`, `semester-store.js`, `ipc-handlers.js`) from the repo-root `lib/` into `packages/core/src/` as the `@lectio/core` package, using `git mv` to preserve history. No logic was changed — the moved files are byte-identical.
- Moved the Vitest suite (`tests/unit/*`, `tests/integration/*`) into `packages/core/tests/` and rewired each test's relative imports from `../../lib/<mod>.js` to `../../src/<mod>.js`.
- Added `packages/core/package.json` (`@lectio/core`) with an `exports` map that preserves the subpath imports the desktop adopts in Phase 2 (`@lectio/core/semester-store`, `@lectio/core/ipc-handlers`), plus `packages/core/vitest.config.mjs` (coverage measured against `src/**`, same 70% line/function thresholds). Removed the now-redundant root `vitest.config.mjs`.
- Delegated the root `test`/`test:watch`/`test:coverage` scripts to the `@lectio/core` workspace.
- Left thin compatibility shims at the old `lib/` paths so the still-rooted desktop keeps running until Phase 2: `semester-store.js`/`ipc-handlers.js` are one-line CommonJS re-exports, and `planner-core.js` is a guarded re-export. Repointed the single `index.html` `<script src>` at `packages/core/src/planner-core.js` (the only renderer edit), since a CommonJS shim cannot serve the browser.

### Mobile preparation — Phase 0: monorepo scaffold + Node 22 baseline

- Introduced npm workspaces at the repo root (`"workspaces": ["packages/*"]`, `"private": true`) and created the empty `packages/core/` and `packages/desktop/` directories (tracked via `.gitkeep`) so the monorepo layout exists before any source moves. No source files were moved — the desktop app builds, runs, and tests exactly as before.
- Bumped the Node baseline to 22: added a root `.nvmrc` pinning Node 22 and an `"engines": { "node": ">=22" }` field in `package.json`.
- Updated CI (`.github/workflows/ci.yml`) and the release workflow (`.github/workflows/release.yml`, both the macOS and Windows jobs) to run on Node 22.
- Updated the stated Node requirement to 22 in `README.md` and `CLAUDE.md`.

## v1.9.0

_Released: 2026-06-15_

- All Courses view: adding a reading or task no longer jumps the list back to the top — the scroll position is preserved across the re-render.
- All Courses view: each course column now has "+ Reading" / "+ Task" buttons with a "to week" picker at the bottom, so you can add a reading or task to any week (including weeks that have no items yet) without using the "+ Add" modal.

## v1.8.8

_Released: 2026-06-04_

- Test release with no functional changes. It exists as a newer self-signed build to update *into* from v1.8.7, verifying end-to-end that macOS auto-update now installs and relaunches (both v1.8.7 and v1.8.8 share the same self-signed certificate, so they have the same designated requirement).

## v1.8.7

_Released: 2026-06-04_

- Fixed macOS auto-update for real. Diagnostics in v1.8.5 confirmed Squirrel.Mac rejected every update because ad-hoc signing gives each build a different designated requirement (`code failed to satisfy specified code requirement(s)`). Builds are now signed with a **persistent self-signed code-signing certificate**, which produces a stable designated requirement (`identifier "com.masprime77.lectio" and certificate leaf = H"…"`), so macOS auto-update installs and relaunches correctly between self-signed builds.
- Added `build/afterPack.js` self-signed signing (env `MAC_SIGN_IDENTITY`, falling back to ad-hoc), a CI keychain-import step in the release workflow (driven by the `MAC_CSC_P12_BASE64` / `MAC_CSC_PASSWORD` secrets), the `scripts/gen-macos-signing-cert.sh` certificate generator, and `docs/MACOS_SIGNING.md`.
- Note: this is not Apple notarization, so the first-launch Gatekeeper prompt remains (handled by the cask / right-click → Open). Updating *into* this first signed build from an older ad-hoc copy still requires a one-time manual reinstall; auto-update works for every release after it.

## v1.8.6

_Released: 2026-06-04_

- Test release with no functional changes. It exists as a newer version to update *into* from v1.8.5, so the v1.8.5 diagnostics (electron-log + inline error surfacing) can capture the exact reason the macOS install/relaunch fails.

## v1.8.5

_Released: 2026-06-04_

- Diagnostic release for the macOS auto-update failure. Added `electron-log` as the `autoUpdater` logger, so the underlying Squirrel.Mac / ShipIt errors are written to `~/Library/Logs/Lectio/main.log` (macOS) / `%APPDATA%\Lectio\logs\main.log` (Windows).
- The update dialog now surfaces auto-update errors inline ("Update failed: …") and re-enables its buttons, instead of leaving a dead "Install & Relaunch" button when an install/relaunch fails.

## v1.8.4

_Released: 2026-06-04_

- Test release that ships no functional changes. Its purpose is to verify the v1.8.3 macOS auto-update fix end-to-end against a real newer release: updating from v1.8.3 should now surface the update dialog, download via the progress bar, and reliably relaunch into v1.8.4 on macOS (and continue to work on Windows).

## v1.8.3

_Released: 2026-06-04_

- Fixed the macOS auto-update never relaunching or applying: `quitAndInstall` closes all windows and calls `app.quit()`, but the unsaved-changes `before-quit` handler could `preventDefault()` and cancel that quit, so Squirrel never swapped the app. The `restart-and-update` handler now allows that quit through, and the renderer flushes pending edits before triggering it.
- Set `autoInstallOnAppQuit = true` as a safety net: a downloaded update now also installs on the next normal quit, so closing and reopening the app picks up the new version even if the explicit relaunch path fails.

## v1.8.2

_Released: 2026-06-04_

- Test release that ships no functional changes; its purpose is to validate the v1.8.1 update flow end-to-end (the `update-available` dialog, the GitHub Release notes panel, the download progress bar, and the reliable relaunch on macOS and Windows) against a real newer release.

## v1.8.1

_Released: 2026-06-04_

- Replaced the auto-update banner with a modal dialog that opens on `update-available`, shows the GitHub Release notes for the new version, and renders a download progress bar.
- When auto-update is off, the dialog lets the user start the download manually via "Download & Install"; when it is on, the download runs in the background and the progress bar shows immediately. After the download completes the primary button becomes "Install & Relaunch".
- Fixed `quitAndInstall` to pass `isSilent` + `isForceRunAfter`, so the update reliably relaunches on macOS and skips the NSIS re-install wizard on Windows.
- Exposed `update-download-progress` and `start-update-download` IPC channels plus `onDownloadProgress`/`startDownload` on the `window.updater` bridge.

## v1.8.0

_Released: 2026-06-03_

- Added IPC handlers for exporting a single course (`export-course`) and a full semester (`export-semester`) to `.lectio.json` files, and for reading a `.lectio.json` file back (`import-file`).
- Added native save/open file-dialog IPC handlers (`show-save-dialog`, `show-open-dialog`) scoped to the `.lectio.json` extension.
- Exposed the new export/import and dialog methods on `window.planner` in the preload bridge.
- Semester export/import now lives in the edit modal footer instead of the header toolbar; the semester selector keeps only the Edit and Delete buttons.
- Export semester writes the full semester (including tag definitions) to a `.lectio.json` file via a native save dialog.
- Import semester reads a `.lectio.json` file and shows a confirmation modal to keep or reset reading/task statuses and, on an id clash, to replace the existing semester or save it as a new one.
- Added drag-and-drop: dropping a `.lectio.json` file onto the window imports it as a semester or a course.
- Added Edit, Export, Import, and Delete icon buttons to each course-column header in Course view.
- Export course writes a single course (without tags) to a `.lectio.json` file via a native save dialog.
- Import course adds a course from a `.lectio.json` file into the current semester with freshly generated ids.
- The header **New** button now opens the create-semester modal directly (no popover).
- The New/Edit modal footer has tab-aware Import/Export buttons: on the Semester tab, Import brings in a full semester and Export (edit mode only) writes the current one; on the Courses tab, Import adds a course to the semester being built or edited; the Tags tab shows neither, since tags can't be imported/exported yet.
- Importing a course from the Courses tab works in both create mode (adds a draft course row, kept with its readings/tasks on save) and edit mode (adds it to the live semester and refreshes the course list).
- Editing the semester from a course column's pencil button or via "+ Add course" now opens the modal on the Courses tab instead of the Semester tab.
- Docs: rewrote the README Features list to match the v1.7.0 feature set (Breakdown panel, focus mode, bulk collapse controls, custom tags, Study Mode, sort control, inline due-date editing, three-tab semester modal, onboarding tour, in-app feedback, per-OS auto-updates).
- Docs: updated the README Download section to use the `Lectio-Setup.exe` artifact name and added the Homebrew one-line install command.
- Docs: added the `icon:win` and `icons` npm scripts to the README icon-build commands.
- Docs: added user stories US-036–US-046 (custom tags, tag management UI, Study Mode, sort control, focus mode, collapsible weeks, breakdown panel, inline due-date editing, onboarding tour, in-app feedback, Windows platform).
- Docs: appended US-036–US-046 to the traceability matrix, corrected the US-036/US-037/US-038 test references to the real test files (`semester-manager.test.js`, `ipc.test.js`, `progress.test.js`, `status.test.js`), and updated the coverage summary to 12 covered / 6 partial / 28 not covered (46 stories).

## v1.7.0

_Released: 2026-06-03_

- Added a "Breakdown" toggle button to the dashboard header that opens an inline panel splitting each course's progress into separate readings and tasks mini-bars with done/total counts, plus a "Total" summary row for the whole semester; the panel respects the current sort order and Study Mode, and the existing progress bars are unchanged.
- Added inline editing of a task's due date directly in the planner: tasks with a date show a clickable "due YYYY-MM-DD" that opens an inline date picker (committing on blur/Enter, cancelling on Escape, clearing the field removes the date), and tasks without one reveal a "＋ date" affordance on row hover to set one.
- Removed the "Reading / Task" quick-add tab from the semester modal (the standalone "Add reading / task" modal is unaffected).
- Renamed the semester modal title in create mode from "Create New Semester" to "New".
- Added an interactive onboarding tour that auto-launches on first run and can be replayed any time from Settings → Start tour: each step spotlights a real UI element with a cutout, shows a titled tooltip, and supports Back/Next/Skip plus keyboard navigation (arrows, Enter, Escape). Steps live in a single `TUTORIAL_STEPS` array so new features only need an entry there (see `docs/TUTORIAL_STEPS.md`).
- Replaced the feedback flow's GitHub redirect with a direct submission to the Vercel `/api/feedback` endpoint, so feedback is sent without leaving the app or needing a GitHub account; the submit button now shows a "Sending…" state, then an in-modal success confirmation, with inline error recovery on failure.

## v1.6.2

_Released: 2026-06-03_

- Added a Windows SmartScreen first-launch warning to the landing page, explaining that the installer is unsigned and describing the "More info → Run anyway" workaround.
- Fixed the broken header logo image: it now points to the always-bundled `assets/icon.png` so it renders in both development and the packaged app.
- Fixed the window being immovable under the `hiddenInset` title bar: the header is now a drag region, with interactive controls opted out so clicks still work.

## v1.6.0

_Released: 2026-06-03_

- Week sections inside each course column (All Courses view) are now collapsible via a chevron toggle; the current week is expanded by default and all others are collapsed, with each section's open/closed state persisted while navigating.
- Added header controls to bulk expand all, collapse all, or expand only the current week; the buttons act on whichever layout is active (Weekly view or All Courses view).
- Course view columns now render at a uniform fixed width (300px) regardless of course name length.
- Long course names are truncated with an ellipsis and reveal the full name via a native tooltip on hover.
- Renamed the header view toggle labels: "Week view" → "Weekly view" and "Course view" → "All Courses" (internal view values unchanged).
- Added a focused single-course mode: clicking a course name in the progress bar isolates that course's column (centred, wider) and dims the other progress rows; clicking it again restores the normal layout.
- Focused mode can also be exited by clicking the empty space around the column or pressing the Esc key.
- Added a global "+ Add" button in the header (shown once a semester is loaded) that opens a modal for adding a reading or task to any course and week of the current semester, with a Reading/Task toggle (Task reveals an optional due-date field) and the current week pre-selected.
- Added a header sort control to order courses by progress (↓/↑) or by their earliest week with content (↑/↓); the choice applies to the progress bars, course columns, and week-view cards, and persists across restarts (localStorage). Sorting is non-destructive — the underlying course order on disk is never changed.
- The sort control bakes a "Sort: " prefix into each option label, so the active mode reads as "Sort: `<mode>`" directly in the select (no separate label element).
- Added alphabetical course sort options (A → Z and Z → A) to the sort control.
- Scoped the progress and alphabetical sorts to the progress bar and All Courses columns only; the Weekly view keeps the original course order for those modes (only week sorts reorder it).
- The Week ↓ sort now also reverses the order of the weeks themselves — weeks render N … 1 in both the Weekly view and inside each All Courses column (Week ↑ stays ascending).
- Week sorts now affect only the order of the weeks (week sections in the Weekly view and within each course column); the progress bar and course-column order fall back to alphabetical (A → Z) instead of ordering courses by their earliest week.
- Replaced the hardcoded reading/task status cycles with a flexible per-semester tag system (data model and core logic): each semester now stores its own `readingTags` and `taskTags`, where every tag has a `section` (`pending`/`done`) that decides whether items count toward course progress. The "pending" and "studied" tags of each kind are protected (cannot be deleted or renamed, but can be recolored). Item statuses now store a tag id rather than a name string.
- Legacy semesters are migrated transparently on load: the default tag sets are added and existing status strings are rewritten to their matching tag ids. (UI for managing tags lands in a later change.)
- The status badge on each reading/task is now a dropdown: clicking it opens a menu of the semester's tags grouped into Pending and Done sections, and picking one assigns that tag to the item.
- The semester editor is now organised into three tabs — Semester, Courses, and Tags — and always opens on the Semester tab.
- The new Tags tab manages each semester's reading and task tags: add a tag to a section, recolor any tag, rename or delete custom tags, and drag to reorder them. Protected tags ("pending"/"studied") keep a locked name, disabled delete, and a fixed position, but can still be recolored.
- Added a Study Mode toggle in the header (persisted across restarts). While on, course progress counts only items tagged "studied" — items with other Done tags no longer contribute — and the button is highlighted; turning it off restores the normal progress calculation.
- While Study Mode is on, the status dropdown gains a distinct green "Studied" shortcut at the bottom for quickly marking an item studied (the studied tag still appears under Done as well).
- Hid the native title-bar text on macOS (`hiddenInset` title bar) so only the traffic-light buttons show, with header padding reserved so content never overlaps them.
- Added a small rounded app logo next to the "Lectio" wordmark in the header.
- Moved theme selection out of the header into the Settings modal as a Light / Dark / Auto segmented control.
- Replaced the floating edit/delete semester icon buttons with a labelled "Edit"/"Delete" group that visually attaches to the semester selector.
- Normalised every interactive header control to a uniform 32px height.
- Merged the "+ Add" and "+ New Semester" header buttons into a single "＋ New" button; the semester modal gains a "Reading / Task" tab for quickly adding a reading or task to the current semester without leaving the modal.
